# 비공개 원본 오라클 매니페스트

이 디렉터리에는 사용자가 보유한 `nox/` 기준본의 **경로, 바이트 수, SHA-256**만 보관한다. `GAME.EXE`, 맵, 음성, 영상 등 원본 자산 자체를 소스 저장소나 공개 CI에 복사하지 않는다.

`nox-2023-1003-01.json`은 다음 규칙으로 봉인한다.

- 모든 일반 파일을 상대 경로 순서로 정렬한다.
- 각 파일의 크기와 SHA-256을 기록한다.
- `(경로 NUL 크기 NUL 파일 SHA-256 LF)` 레코드 전체의 SHA-256을 `tree_sha256`으로 기록한다.
- 심볼릭 링크, 특수 파일, 대소문자만 다른 충돌 경로와 이식 불가능한 경로는 거부한다.
- `seal`은 기존 매니페스트를 덮어쓰지 않는다. 기준본을 바꾸려면 기존 파일을 보존하고 새 ID와 새 파일명으로 별도 검토한다.

검증은 저장소 루트에서 실행한다.

```sh
make oracle-verify
make oracle-code-verify
make oracle-test
```

`game-exe-functions.json`은 포팅한 원본 함수의 VA, 정확한 함수 길이와 범위 SHA-256을 기록한다. 검증기는 전체 트리 매니페스트와 오라클 ID 및 `GAME.EXE` 해시를 교차 확인하고, PE32/I386 형식, 이미지 베이스 `0x00400000`, 함수와 jump table이 실행 섹션에 있는지, 상수 테이블이 비실행 섹션에 있는지와 각 범위 해시를 검사한다. 현재는 `0x004D6A20`, `0x004D8270`, `0x004D9EB0`, SpellProjectile 의존 함수 `0x004E03D0/0x004E0A70`, 주소 순서 복원 `0x004E44F0..0x004ECE9F`, Bomb/Boom/Die/Glyph/SparkExplosion/WallReflect/Pixie/WallReflectSpark/OwnCollide/Spark/Webbing/Flag/Barrel/AudioEvent/Pentagram/Sign/TrapDoor/Teleport/AwardSpell/Fist/TeleportWake/Chakram/Arrow/Harpoon/MonsterArrow/BearTrap/PoisonGasTrap/GameBall carrier state writer/Ball/Crown/HomeBase/UndeadKiller/MonsterGenerator/SoulGate/Ankh collision, `0x004EEBF0..0x004EEC7C`, Chakram bolt damage `0x004EF1E0..0x004EF26F`, CrownPickup `0x004F3400..0x004F34CF`, SpellProjectile이 도달하는 `0x004FA2B0..0x004FA58E`, Boom/Pixie/Chakram 방향 함수 `0x00509ED0..0x00509F1F`, `0x00536D80..0x00536E73`, MonsterArrow parser `0x00536E80..0x00536EBF`, `0x00537760..0x0053776E`, Chakram update `0x0053DCC0..0x0053DDEF`, CrownUpdate `0x0053E1D0..0x0053E2AF`, `0x0054E170..0x0054E45D`, DeathBall Door 반사 `0x0057B770..0x0057B801`과 shared 벽 반사 `0x0057B810..0x0057B841`을 합계 307개 코드 범위로 봉인했다.

위 주소 나열의 `307`은 그 문단을 작성한 시점의 중간 집계다. 최신 매니페스트는 GlyphDrop·TrapDrop·CrownDrop·TreasureDrop과 object drop dispatcher `004ED500..004ED80F`, Treasure의 Scavenger report 의존성과 관련 registration/name 영역까지 포함해 **349개 코드 범위와 168개 비실행 데이터 범위**를 검증한다. 아래의 더 오래된 누적값은 단계별 이력이며 마지막 최신 문단을 판정 기준으로 삼는다.

기존 방향 벡터·임계값·반환표, collision 상수와 표·문자열·등록 레코드에 더해 SpellProjectile의 벽 반사 `0.0f`, projectile/DeathBall 반사 `0.1f`, InversionEffect부터 TeleportWake까지 복원한 collision 등록 레코드와 NUL 이름, Boom의 방향 상수와 다섯 balance 문자열, SparkExplosion의 정확한 반경 상수와 parser 형식 문자열, Chest의 SilverKey·잠금 메시지 블록, Spark와 Webbing이 각각 참조하는 별도 WebbingSlow 메시지, WallReflect/WallReflectSpark/Pixie shared parser 형식, AudioEvent의 `%s` parser 형식, DeathBall의 32쌍 Door 방향표와 두 balance 문자열을 봉인했다. 여기에 Chakram의 binary64 `0.5`, binary32 공격 반경 `30`, retarget epsilon·거리 상수, 256쌍 binary32 방향표, random 호출의 원본 debug 경로, `ArcherBolt`·`BoltSoloDamageMin` 문자열, collide/update 등록 레코드와 두 NUL 이름을 추가했다. Arrow가 공유하는 binary64 `0.5`, 별도 위치의 `ArcherBolt` 문자열, collide 등록 레코드와 NUL 이름, Harpoon의 `HarpoonDamage` balance 키·collide 등록 레코드·NUL 이름, MonsterArrow의 collide 등록 레코드·NUL 이름과 parser가 각각 참조하는 두 개의 별도 NUL space delimiter, BearTrap의 `ClosedBearTrap` type key·collide 등록 레코드·NUL 이름, PoisonGasTrap의 `ToxicCloud` type key·`ToxicCloudLifetime` balance key·collide 등록 레코드·NUL 이름, GameBall의 BallUpdate registration record·NUL 이름, Ball collision의 `CantPickupBall` message·registration record·NUL 이름, Crown collision/pickup/update의 세 registration record와 세 NUL 이름, HomeBase의 `GameBall`·`GameBallStart` type key·random debug path·registration record·NUL 이름, UndeadKiller와 MonsterGenerator의 registration record와 NUL 이름, SoulGate의 registration record·NUL 이름·priority message, Ankh의 registration record·NUL 이름·`ExtraLifeAlreadyAwarded`/`MaxExtraLives`/`MaxTradableAnkhsReached`/`AnkhTradable`/`AwardExtraLife` 문자열을 더했다. 이후 `Respawn`, default-team pointer/name block, material `(name, ID)` row/name/`NO TEAM`, `TeamBase` 문자열을 추가해 합계 156개 비실행 데이터 범위를 검사한다. `004E8E50`이 반환하는 `0x007531F8`, Chakram retarget scratch `0x007531F0..0x00753250`, Chest와 Ankh가 공유하는 feedback ticks `0x00753258`, Harpoon damage cache `0x00753298`, Ball collision feedback frame `0x0075329C`, Ankh reset buffers `0x007532A0/0x007532A4`, Chakram과 DeathBall이 공유하는 trace point `0x00833EB8`과 full ready dword `0x00833EC0`, OwnCollide·BarrelCollide·AudioEventCollide·ChakramUpdate·GameBall carrier state writer·CrownPickup·SoulGateCollide·AnkhCollide가 읽는 frame `0x0084EA04`, Webbing·ChakramUpdate·PoisonGasTrapCollide·SoulGateCollide·AnkhCollide가 읽는 FPS dword `0x0085B3FC`는 PE의 초기화 데이터 범위 밖 BSS이므로 별도 데이터 해시 범위를 만들지 않는다.

`004EAD50`은 전체 실행 코드와 절대 포인터 스캔에서 참조가 없었던 구형 1-mana transfer 함수이므로 등록·호출 경계를 새로 만들지 않았고, 직접 의존하는 `00418AB0/00419130/00419180/004EEB80` 본체의 전역 복원까지 주장하지 않는다. 등록 테이블 소비자 `00536EC0`은 name 기준 16바이트 record의 callback/data-size/parser를 `+4/+8/+12`에서 읽으며 Chakram collision record `005CA418`은 callback `004EAF00`, data size 0을, Arrow collision record `005CA428`은 callback `004EB490`, MonsterArrow collision record `005CA438`은 callback `004EB800`, 원본 data size 8, parser `00536E80`을, BearTrap collision record `005CA448`은 callback `004EB890`, PoisonGasTrap collision record `005CA458`은 callback `004EB910`, 두 record 모두 data size 0과 null parser를, Harpoon collision record `005CA4D8`은 callback `004EB6A0`과 원본 collide-data size 8을 결속한다. Arrow와 Harpoon collide-data의 두 번째 필드가 객체 포인터이므로 두 native record는 64비트에서 16바이트지만 MonsterArrow의 두 signed int32 field는 모든 pointer 폭에서 8바이트다. MonsterArrow parser는 두 번째 `strtok`에도 원래 args pointer를 전달해 첫 token을 두 field에 반복 저장하는 원본 결함을 보존한다. BearTrap은 target nil을 source보다 먼저 단락하고 shared gate의 모든 nonzero를 허용한다. `ClosedBearTrap` 할당 성공 뒤에만 source의 Y, X, owner를 그 순서로 읽어 원본 `CreateAt`의 다섯 번째 reserved zero slot과 함께 생성하고, source 지연 삭제 뒤 Held `5/90/5`, Anchored `14/90/5`, source audio `846`을 순서대로 적용한다. PoisonGasTrap도 target-first·any-nonzero gate와 allocation-success 뒤 Y→X→owner 생성 순서를 공유하지만, 생성 뒤 live update-data를 읽고 `ToxicCloudLifetime`과 signed FPS를 x87 53-bit precision에서 곱한 뒤 한 번 binary32로 spill하고 nearest-even int32 duration을 저장한다. 이어 source audio `847`과 지연 삭제를 실행한다. 별도 update table의 `005C9308` record는 이름 `005C9718`, callback `0053DCC0`, 원본 update-data size 28을 결속한다. 이 record 안의 두 객체 포인터 때문에 native update record는 64비트에서 40바이트이고, Chakram과 Arrow 충돌 중 임시 attack record도 원본 32바이트에서 native 48바이트로 넓힌다. Harpoon이 접근하는 `PlayerUpdateData`의 target/bolt/field35/X/Y/frame은 32비트 `132/136/140/144/148/152`, 64비트 `152/160/168/172/176/180`이다. PoisonGasTrap이 쓰는 `ToxicCloudUpdateData`는 pointer 없이 모든 폭에서 4바이트이고 duration offset은 0이다. Fist callback identity consumer와 producer/update, TeleportWake producer와 공유 gate의 다른 상위 호출자, Chest 하위 효과, Pentagram 전체 update record, trace helper의 다른 raw C caller는 계속 별도 widening 범위다. Chakram callback·update·retarget·random-reflect·bolt-damage와 Arrow·Harpoon·MonsterArrow·BearTrap·PoisonGasTrap callback 및 MonsterArrow parser의 의미와 typed 경계는 복원했지만 inventory detach/put/equip, item/player attack effects, map damage, movement update와 일부 객체 생성·삭제·buff·audio·balance 서비스의 내부 ABI32까지 전역 완료로 주장하지 않는다. 특히 Arrow producer `00539D80`의 주변 매개변수와 지역 변수, attack-effect raw C 서비스는 아직 ABI32 가정을 포함한다. 함수 하나를 포팅할 때마다 어셈블리에서 경계를 확인한 뒤 이 목록과 의미 설명을 확장하며, 다음 순차 함수는 `sub_4EB9B0`이다.

별도 update table의 `005C9338` record는 이름 `005C974C`, callback `0053DF40`, 원본 update-data size 32와 null parser를 결속한다. `BallUpdate\0` 이름과 record를 각각 독립 데이터 범위로 봉인했다. GameBall carrier state writer `004EB9B0`은 ball update-data를 target보다 먼저 cache하고 owner-chain lookup의 nil 또는 terminal 객체를 그대로 반환한다. Player bit 성공 경로에서는 carrier pointer, live team ID 한 바이트의 zero-extended dword, live frame을 순서대로 저장하고, 모든 거부 경로에서는 carrier와 team만 지워 frame을 유지한다. native `GameBallUpdateData`의 `size/Carrier/TeamID/Ticks/CarrierFrame/PossessionDuration/ResetVelocity/Reserved`는 32비트 `32/0/4/8/16/20/24/28`, 64비트 `40/0/8/16/24/28/32/36`이며 live 함수는 두 native object pointer와 pointer 반환의 typed Go export다. 다만 callback을 부르는 상위 여섯 경로의 나머지 ABI32 내부와 원본 32바이트 layout을 고정 오프셋으로 소비하는 raw Ball updater `0053DF40`은 아직 전역 widening 대상이다. 전체 오라클 집계는 코드 239개·데이터 121개이고 다음 순차 함수는 Ball collision `004EBA00`이다. 위 문단의 마지막 `sub_4EB9B0` 다음 함수 표기는 이 갱신으로 대체한다.

collision table의 `005CA478` record는 이름 `005CA828`, callback `004EBA00`, data size 0과 null parser를 결속한다. `BallCollide\0`, `objcoll.c:CantPickupBall\0`, record를 각각 독립 데이터 범위로 봉인했다. Ball collision은 entry update-data cache와 target의 live team/cached carrier 거부, unsigned 45-frame feedback throttle, owner/class·owned-list 단락, existing/create-team 분기, entry team pointer 기준 BallStatus, carrier writer→audio→live flag→buff purge 순서를 보존한다. callback은 세 native pointer의 typed Go export이며 Object/ObjectTeam/Team/GameBall의 32/64비트 배치를 검사한다. feedback frame `0075329C`는 BSS라 해시하지 않는다. team 변경/생성 본체 `004196D0/004191D0`, raw Ball updater `0053DF40`과 상위 caller는 별도 widening 대상이다. 전체 오라클 집계는 코드 241개·데이터 124개이고 다음 순차 함수는 `sub_4EBB50`이다. 위 문단의 마지막 Ball collision 다음 함수 표기는 이 갱신으로 대체한다.

collision table의 `005CA498` record는 이름 `005CA844`, callback `004EBB50`, data size 0과 null parser를 결속한다. pickup table `005C987C` record는 이름 `005C9968`, 네 인수 callback `004F3400`, null parser를, update table `005C9348` record는 이름 `005C9758`, callback `0053E1D0`, 원본 data size 12와 null parser를 결속한다. Crown collision은 target nil·flags `0x8020`·저위 Player bit guard에서 full target pointer를 유지하고 성공 시 `(target,crown,1,1)` pickup의 32비트 결과를 반환한다. CrownPickup은 entry update-data cache, non-Player pending 보존, 성공 효과와 Player 경로의 cached pending last-clear를 보존한다. CrownUpdate는 cached pending early pickup, disabled fallback과 blocked owner clear, radius 합 `+10`, direction 두 번 읽기, trace flags 5와 conditional move를 보존한다. native `CrownUpdateData`는 32/64비트 `12/24`바이트이고 PickupTarget offset은 `4/8`이다. 세 callback/update와 active producer store는 typed native Go/CGo 경계다. default pickup·enchant·audio·network/team·trace/move service와 나머지 상위 caller는 별도 widening 대상이다. 전체 오라클 집계는 코드 247개·데이터 130개이고 다음 순차 함수는 HomeBase collision `004EBB80`이다. 위 문단의 마지막 `sub_4EBB50` 다음 함수 표기는 이 갱신으로 대체한다.

collision table의 `005CA488` record는 이름 `005CA834`, callback `004EBB80`, data size 0과 null parser를 결속한다. HomeBase collision `004EBB80..004EBD38`과 7바이트 padding, distinct `GameBall\0`·`GameBallStart\0`, random debug path와 `HomeBaseCollide\0`를 독립 범위로 봉인했다. 두 lookup 선행, full 32비트 guard return, cached update/live carrier reload, nil 포함 team pointer equality, wrapping lesson 증가, observer/audio/effect, 두 live marker traversal, count 0의 random maximum `-1`, clear-owner→move→effect→네 zero store와 full effect return을 보존한다. callback은 `uint32_t(nox_object_t*,nox_object_t*,float*)` typed native Go/CGo 경계이고 Object/GameBall/PlayerUpdate/Team의 32/64비트 배치를 검사한다. 전체 오라클 집계는 코드 249개·데이터 135개이고 다음 순차 함수는 UndeadKiller collision `004EBD40`이다. observer와 score/team/audio/list/random/move/effect service 내부 및 나머지 상위 caller는 별도 widening 대상이다. 위 문단의 마지막 HomeBase 다음 함수 표기는 이 갱신으로 대체한다.

collision table의 `005CA4A8..005CA4B7` record는 이름 `005CA854`, callback `004EBD40`, 원본 collide-data size 4와 null parser를 결속한다. UndeadKiller collision `004EBD40..004EBE0C`, 3바이트 NOP padding `004EBE0D..004EBE0F`, record와 정확한 `UndeadKillerCollide\0` 이름을 독립 범위로 봉인했다. callback은 target nil에서 collision도 nil인 경우에만 source를 지연 삭제하고, non-nil target에서는 저위 Monster·Undead bit를 순서대로 검사한다. 통과 경로는 source collide-data를 HP 호출 전에 cache하고 HP를 uint16으로 읽은 뒤 signed remaining을 비교한다. partial 분기는 parent lookup 뒤 target의 live Damage에 HP를 type 6으로 적용하고 cached entry remaining에서 HP를 빼며, consuming 분기는 remaining 자체를 적용하고 source 삭제 뒤 live spell remaining을 재조회해 cached entry에서 뺀다. remaining 0은 Damage와 store 없이 삭제만 한다. `UndeadKillerCollideData size/Spell=4/0`(32비트)·`8/0`(64비트), `DurSpell size/Field72/Flags88=120/72/88`·`184/100/120`이며 raw ABI32 callback은 세 native pointer의 typed Go/CGo export로 교체했다. 전체 오라클 집계는 코드 251개·데이터 137개이고 다음 순차 함수는 MonsterGenerator collision `004EBE10`이다. TurnUndead producer `00531310`, delete-side consumer `00531420`, update `0053E190`, identity consumer `00537610`과 호출 service 내부는 별도 widening 대상이다. 위 문단의 마지막 UndeadKiller 다음 함수 표기는 이 갱신으로 대체한다.

collision table의 `005CA4E8..005CA4F7` record는 이름 `005CA8A0`, callback `004EBE10`, data size 0과 null parser를 결속한다. MonsterGenerator collision `004EBE10..004EBE36`, 9바이트 NOP padding `004EBE37..004EBE3F`, record와 정확한 `MonsterGeneratorCollide\0` 이름을 독립 범위로 봉인했다. callback은 target nil·class 저위 Player bit를 source 접근 전에 검사하고 통과 뒤 live source update-data의 원본 offset 72 callback block을 target caller/source trigger로 호출하며 collision과 반환을 버린다. 원본 stack call은 block/target/source 세 인수이고 OpenNox 어댑터의 event 19는 기존 script service용 런타임 메타데이터다. native `MonsterGenUpdateData size/ScriptCollision=164/72`(32비트)·`216/120`(64비트), `ScriptCallback size/Flags/Func=8/0/4`이며 raw ABI32 callback은 세 native pointer의 typed Go/CGo export로 교체했다. 전체 오라클 집계는 코드 253개·데이터 139개이고 다음 순차 함수는 SoulGate collision `004EBE40`이다. MonsterGenerator create/init/update/xfer와 원본 size 164 update registration은 계속 ABI32이며 collision 단위 복원을 전체 64비트 완료로 세지 않는다. 위 문단의 마지막 MonsterGenerator 다음 함수 표기는 이 갱신으로 대체한다.

collision table의 `005CA4F8..005CA507` record는 이름 `005CA8B8`, callback `004EBE40`, 원본 collide-data size 4와 null parser를 결속한다. SoulGate collision `004EBE40..004EBF3C`, 3바이트 NOP padding `004EBF3D..004EBF3F`, record, 정확한 `SoulGateCollide\0` 이름과 `objcoll.c:SoulGateCollide\0` priority message를 독립 범위로 봉인했다. callback은 source collide-data를 Quest flag보다 먼저 cache하고 Quest/target/저위 Player bit gate 뒤 모든 live player unit을 끝까지 순회한다. cached Player pointer와 `Field4792 == 1`을 통과한 non-nil SoulGate가 없을 때만 frame timer를 호출한다. 이어 target update-data를 cache하고 다른 gate 또는 unsigned wrapping frame delta가 live FPS보다 엄격히 큰 같은 gate에서 audio→point effect→message를 실행한 뒤 target gate와 새 live source timestamp를 그 순서로 저장한다. native `Object size/ObjClass/PosVec/CollideData/UpdateData=780/8/56/700/748`(32비트)·`928/12/60/776/872`(64비트), `PlayerUpdateData size/Player/SoulGate=556/276/308`·`640/320/376`, `Player size/Field4792=4828/4792`·`6136/6096`, `SoulGateCollideData size/LastUsedFrame=4/0`이며 raw ABI32 callback은 세 native pointer의 typed Go/CGo export로 교체했다. 전체 오라클 집계는 코드 255개·데이터 142개이고 다음 순차 함수는 Ankh collision `004EBF40`이다. 호출 서비스 내부와 기존 플랫폼별 전체 제품 차단점은 별도 widening 대상이며 SoulGate collision 단위 복원을 전체 64비트 완료로 세지 않는다. 위 문단의 마지막 SoulGate 다음 함수 표기는 이 갱신으로 대체한다.

collision table의 `005CA508..005CA517` record는 이름 `005CA8C8`, callback `004EBF40`, collide-data size 0과 null parser를 결속한다. Ankh collision `004EBF40..004EC287`, 8바이트 NOP padding `004EC288..004EC28F`, record, 정확한 `AnkhCollide\0` 이름과 다섯 직접 문자열을 독립 범위로 봉인했다. callback은 source InitData를 target guard보다 먼저 cache하고 Player의 다섯 native slot과 64개 고정폭 history record를 검사한다. 각 record의 live FPS/frame, unsigned wrapping strict expiration, name→serial byte→class→frame reset과 class/name/serial 사이의 live Player 재로드를 보존한다. exact history match의 duplicate slot insertion, strict 1500 ms feedback, signed extra-life maximum과 x87 nearest-even 변환, nil object에도 계속되는 award side effects, live Player/Next ring store 순서도 계약으로 고정했다. `Object size/ObjClass/PosVec/Field34/InitData/Pickup/UpdateData=780/8/56/136/692/708/748`(32비트)·`928/12/60/140/760/792/872`(64비트), `PlayerUpdateData size/Player/ExtraLives=556/276/320`·`640/320/400`, `Player size/Serial/info/Field4792/QuestAnkhs=4828/2112/2185/4792/4796`·`6160/2116/2189/6096/6104`, `AnkhHistoryRecord size/Name/Class/Serial/Frame=80/0/50/51/76`, `AnkhInitData size/Next=5124/5120`이다. 5,124바이트 크기는 collision row가 아니라 별도 Ankh init registration이 native type에서 산출한다. raw ABI32 callback은 세 native pointer의 typed Go/CGo export로 교체했다. 전체 오라클 집계는 코드 257개·데이터 149개이고 다음 순차 함수는 owner setter `004EC290`이다. 호출 서비스 내부와 기존 플랫폼별 전체 제품 차단점은 별도 widening 대상이며 Ankh collision 단위 복원을 전체 64비트 완료로 세지 않는다. 위 문단의 마지막 SoulGate 다음 함수와 누적 집계 표기는 이 갱신으로 대체한다.

owner setter 본체 `004EC290..004EC2F9`와 6바이트 NOP padding `004EC2FA..004EC2FF`를 독립 범위로 봉인했다. 본체와 padding의 SHA-256은 `ef4b3785482795017fe3eebf96079f2416ede27a6c2b112c4fa4d6397f0ee4e3`, `ff35ffe14925642da6f3a258b35811e08101c03f8b5db346e5afcca448677564`이고 little-endian 절대 함수 포인터는 없다. decoded direct rel32 call 25곳은 `004DAAF8/004DAC31/004DB67F/004DB72A/004E194E/004E1BD6/004E1C8A/004E7B50/004E9B12/004EA2ED/004EBAC0/004EC4D2/004ED5C9/004F30D4/004F3452/005014FA/005144AE/005144FB/00514550/005145CB/00516751/00516F7B/00516FEC/0052BE89/0054A1AA`다. `(owner, object)` 함수는 nil object 무효과, clear-owner 선행, live Destroyed-owner traversal, `next-owned -> first-owned -> owner` 저장, 두 class load 사이 Monster callback과 후속 player-mask callback 순서를 보존한다. `Object size/ObjClass/ObjFlags/ObjOwner/Field128/Field129=780/8/16/508/512/516`(32비트)·`928/12/20/552/560/568`(64비트)이며 기존 두 native pointer typed CGo export를 유지한다. 전체 오라클 집계는 코드 259개·데이터 149개이고 다음 순차 함수는 clear-owner `004EC300`이다. clear-owner와 두 callback 내부 및 기존 플랫폼별 전체 제품 차단점은 별도 widening 대상이다. 위 문단의 마지막 Ankh 다음 함수와 누적 집계 표기는 이 갱신으로 대체한다.

clear-owner 본체 `004EC300..004EC3DF` 224바이트를 SHA-256 `3301d9b287672bfe80bcf084940ce0261acb3b4ff2bfdea572f4f1627771590b`으로 봉인했다. 다음 함수가 `004EC3E0`에서 바로 시작하므로 padding은 없고, little-endian 절대 함수 포인터와 direct jump도 없다. decoded direct rel32 call 25곳은 `00418046/004DAE18/004E12AD/004E1947/004E1BCF/004E1C83/004E5E8C/004E694C/004E7BAE/004E9B0B/004EAA34/004EBD07/004EC29A/004ED220/004ED6A1/004F7E56/005014ED/0050A513/005147EC/0051677C/0052BE82/0052C144/0053E06C/0053E0B1/0053E22C`다. 함수는 nil object·nil entry owner를 단락하고 entry owner가 Player이며 monitor 판정이 참일 때 callback 뒤 live owner를 재로드해 object subclass 저위 바이트의 `0x80`만 지운다. 그 owner의 update-data를 cache한 뒤 Player와 PlayerInd를 각 효과 직전에 같은 cache에서 재로드해 shield unmonitor와 flags 1 minimap unmark를 순서대로 호출한다. 이후 live owner를 다시 재로드해 first-owned chain을 predecessor와 함께 순회하고 object가 없으면 마지막 predecessor를 terminal로 사용해 predecessor 또는 head를 object의 live next로 잇되 object next 자체는 지우지 않는다. 마지막에는 object class 저위 바이트를 cache하고 owner를 nil로 만든 뒤 cached Monster bit callback, 독립적으로 재로드한 live Player/Monster mask callback을 순서대로 호출한다. `Object size/ObjClass/ObjSubClass/ObjOwner/Field128/Field129/UpdateData=780/8/12/508/512/516/748`(32비트)·`928/12/16/552/560/568/872`(64비트), `PlayerUpdateData size/Player=556/276`·`640/320`, `Player size/PlayerInd=4828/2064`·`6160/2068`이며 기존 한 native pointer typed CGo export를 유지한다. 전체 오라클 집계는 코드 260개·데이터 149개이고 다음 순차 함수는 `nox_xxx_playerObserverFindGoodSlave2_4EC3E0`이다. 두 효과 callback과 owner-clear 후 callback 내부 및 기존 플랫폼별 전체 제품 차단점은 별도 widening 대상이다. 위 문단의 마지막 owner setter 다음 함수와 누적 집계 표기는 이 갱신으로 대체한다.

first-good-slave 본체 `004EC3E0..004EC417` 56바이트와 NOP padding `004EC418..004EC41F` 8바이트를 각각 SHA-256 `67a384f7958f6a3e19ebe15973f1e7707063f1f958a047e3540517e314f04d43`, `9e8376b4aa602de084708bf231f7ab5bd700e3d623bcf47a3851ce49cbe46f08`로 봉인했다. 다음 함수는 `004EC420`이고 little-endian 절대 함수 포인터와 direct jump는 없다. decoded direct rel32 call은 이미 Go로 복원된 observer usable-slave 본체의 `004E6297/004E62CF` 두 곳뿐이다. nil owner는 first-owned를 읽기 전에 반환하고, 후보의 class 저위 Monster bit가 없으면 update-data를 읽지 않으며, Monster이면 update-data status 저위 summoned bit를 검사한다. 성공 후보는 next를 읽지 않고 즉시 반환하며 실패 후보만 live next-owned를 읽고, nil Monster update-data는 원본처럼 fault한다. 원시 C의 pointer-to-`int` 구현·선언은 실행 경계에서 제거했고 두 caller가 Go 소유라 새 CGo export는 만들지 않았다. `Object size/ObjClass/Field128/Field129/UpdateData=780/8/512/516/748`(32비트)·`928/12/560/568/872`(64비트), `MonsterUpdateData size/StatusFlags=2200/1440`·`2824/2048`이다. 전체 오라클 집계는 코드 262개·데이터 149개이고 다음 순차 함수는 `nox_xxx_playerObserverFindGoodSlave_4EC420`이다. 기존 플랫폼별 전체 제품 차단점은 별도 widening 대상이다. 위 문단의 마지막 clear-owner 다음 함수와 누적 집계 표기는 이 갱신으로 대체한다.

next-good-slave 본체 `004EC420..004EC461` 66바이트와 NOP padding `004EC462..004EC46F` 14바이트를 각각 SHA-256 `fd7a3794038870c6b320b99a355954fc1f7717c5cf2ebf143374d734b7ff3a7b`, `e2dac2a3e4166130a2801c775fbc9d722fbafd40c777e11c307e3e69c0feaffc`로 봉인했다. 다음 함수는 `004EC470`이고 little-endian 절대 함수 포인터와 direct jump는 없다. decoded direct rel32 call은 이미 Go로 복원된 observer usable-slave 본체의 `004E629F/004E62B6/004E62E1` 세 곳뿐이다. nil current는 owner를 읽기 전에 반환하고, non-nil current는 owner `+508`을 존재 여부로만 한 번 검사해 nil이면 next를 읽지 않는다. owner가 있으면 current next `+512`에서 시작하며 후보 owner 일치 여부는 검사하지 않는다. 이후 class/status 저위 바이트의 Monster `0x02`·summoned `0x80`, non-Monster data 미조회, 성공 후보 next 미조회, 실패 뒤 live next와 nil Monster data fault를 first-good-slave와 같은 순서로 보존한다. 원시 C의 pointer-to-`int` 구현·선언을 실행 경계에서 제거했고 세 caller가 Go 소유라 새 CGo export는 만들지 않았다. `Object size/ObjClass/ObjOwner/Field128/UpdateData=780/8/508/512/748`(32비트)·`928/12/552/560/872`(64비트), `MonsterUpdateData size/StatusFlags=2200/1440`·`2824/2048`이다. 전체 오라클 집계는 코드 264개·데이터 149개이고 다음 순차 함수는 `nox_xxx_unitRemoveChild_4EC470`이다. 기존 플랫폼별 전체 제품 차단점은 별도 widening 대상이다. 위 문단의 마지막 first-good-slave 다음 함수와 누적 집계 표기는 이 갱신으로 대체한다.

remove-child 본체 `004EC470..004EC4A4` 53바이트와 NOP padding `004EC4A5..004EC4AF` 11바이트를 각각 SHA-256 `4a70d7368491ee2c4d81d77b5805ae584a9c3e3658e90ca8ef91aaeb729f60b8`, `19f3c2045194c5d2e45451e3dfe6a203b5e240aec5a2400a92cdb425c3331137`로 봉인했다. 다음 함수는 `004EC4B0`이고 little-endian 절대 함수 포인터와 direct jump는 없다. decoded direct rel32 call은 이미 Go로 복원된 `ObjectDeleteLast`의 `004DAE1E` 한 곳뿐이다. nil parent는 first-owned를 읽기 전에 반환한다. non-nil parent는 first-owned `+516`을 읽고, 각 non-nil child에서 next-owned `+512`를 먼저 cache한 뒤 owner `+508`, next-owned `+512`를 그 순서로 0으로 쓰며 cached successor를 따른다. 마지막에는 parent first-owned를 0으로 쓰고, 처음부터 빈 목록이어도 이 마지막 store를 생략하지 않는다. child owner 일치 여부나 class/flags/data는 읽지 않으며 self-cycle은 지워진 node를 cached pointer로 한 번 더 방문한 뒤 종료한다. raw C 본체는 provenance-only `#if 0`로 남기고 헤더·CGo wrapper를 실행 경계에서 제거했으며 Go 소유 caller를 native `Object`에 직접 결속했다. `Object size/ObjOwner/Field128/Field129=780/508/512/516`(32비트)·`928/552/560/568`(64비트)이다. 전체 오라클 집계는 코드 266개·데이터 149개이고 다음 순차 함수는 `nox_xxx_unitTransferSlaves_4EC4B0`이다. 기존 플랫폼별 전체 제품 차단점은 별도 widening 대상이다. 위 문단의 마지막 next-good-slave 다음 함수와 누적 집계 표기는 이 갱신으로 대체한다.

transfer-slaves 본체 `004EC4B0..004EC4E2` 51바이트와 NOP padding `004EC4E3..004EC4EF` 13바이트를 각각 SHA-256 `286cd1bdb61b7de2bdcc15b753183a90430662a3763b035e198e0e5f812fb9c2`, `aff312c80e826834eed3e424180d0b1150cd49ab4454e19d6d9cd884a2178915`로 봉인했다. 합친 64바이트 SHA-256은 `4ce88500c8e12f139e7cee1aff3b96ea9e921bbd7744e8cab2fc6dece50e0ced`이고 다음 함수는 `004EC4F0`이다. little-endian 절대 함수 포인터와 direct jump는 없으며 decoded direct rel32 call은 삭제 완료의 `004E5E86`과 몬스터 사망의 `0050A50D` 두 곳이다. nil source는 메모리를 읽지 않고, non-nil source는 first-owned `+516`을 한 번 읽는다. 빈 목록이면 owner를 읽거나 setter를 부르지 않고 head도 다시 쓰지 않는다. 각 child에서는 next-owned `+512`를 먼저 cache한 뒤 source owner `+508`을 매 반복 live reload하고 owner setter `004EC290(owner, child)`를 호출한 다음 cached successor를 따른다. 본체에는 source head를 직접 지우는 store가 없으며 정상 owned-list에서는 setter의 clear/insert 과정이 source 목록을 수선한다. raw C 본체는 provenance-only `#if 0`로 남기고 Go 소유 삭제 caller는 native 서버 메서드에 직접 결속했으며, `GAME4_1.c`에 남은 몬스터 사망 caller는 `nox_object_t*` 하나의 typed Go CGo export를 사용한다. `Object size/ObjOwner/Field128/Field129=780/508/512/516`(32비트)·`928/552/560/568`(64비트)이다. 전체 오라클 집계는 코드 268개·데이터 149개이고 다음 순차 함수는 `nox_xxx_unitHasThatParent_4EC4F0`이다. 기존 플랫폼별 전체 제품 차단점은 별도 widening 대상이다. 위 문단의 마지막 remove-child 다음 함수와 누적 집계 표기는 이 갱신으로 대체한다.

owner-chain identity 본체 `004EC4F0..004EC516` 39바이트와 NOP padding `004EC517..004EC51F` 9바이트를 각각 SHA-256 `0121a9f7223c128b103e28bf0036a9c1d9a6bce57ac60144ee4ac0b5f424902e`, `f56642978961c41b24911838d549a9957c25a0dee0914c9230b5f17a3567418b`로 봉인했다. 합친 48바이트 SHA-256은 `df3f3d179fddc590fc696155d1fc7b99b288cf484ececd069155b9000cb48b05`이고 다음 함수는 `004EC520`이다. little-endian 절대 함수 포인터와 direct jump는 없으며 decoded direct rel32 call은 `004E5F6E/004F351C/00500CF5/00514610/0051467B/005146F9/0051479D/005194B1/0052CCFC/00537F43/0053BD3C` 11곳이다. nil object 또는 nil target은 owner를 읽기 전에 false를 반환하고, 시작 object 자신부터 target과 비교하며 실패할 때만 live owner `+508`을 읽어 terminal까지 진행한다. target을 포함하지 않는 cycle은 원본처럼 종료를 보장하지 않는다. 기존 native `Object.HasOwner`를 이 순서에 결속하고 C에 남은 네 caller에는 두 native pointer와 정확한 32비트 `C.int` 반환의 CGo export를 유지했다. `Object size/ObjOwner=780/508`(32비트)·`928/552`(64비트)이다. 전체 오라클 집계는 코드 270개·데이터 149개이고 다음 순차 원본 감사 함수는 이미 별도 의존성으로 봉인된 `nox_xxx_unitsHaveSameTeam_4EC520`이다. 기존 플랫폼별 전체 제품 차단점은 별도 widening 대상이다. 위 문단의 마지막 transfer-slaves 다음 함수와 누적 집계 표기는 이 갱신으로 대체한다.

same-team 본체 `004EC520..004EC572` 83바이트와 NOP padding `004EC573..004EC57F` 13바이트를 각각 SHA-256 `9642c230874e50fde90809defb3633c1eff1f8f22e7c08be49816f640e22dae1`, `aff312c80e826834eed3e424180d0b1150cd49ab4454e19d6d9cd884a2178915`로 봉인했다. 합친 96바이트 SHA-256은 `f03bd5968018902d142c979eeedd95151d2109afba47879e872c2ab5e5f72a4a`이고 다음 함수는 `004EC580`이다. little-endian 절대 함수 포인터와 direct jump는 없으며 decoded direct rel32 call은 `004E6F0A/004E973F/004E99C0/004E9A64/004E9DEE/004EB088/004FE025/0052E707/00548585/0054E70D/0054E7AA/0054E7EF` 12곳이다. 어느 인수든 nil이면 객체 필드를 읽기 전에 0을 반환한다. non-nil이면 왼쪽 owner chain의 각 객체에서 embedded team-record 주소 `+48`을 한 번 cache하고 오른쪽 chain을 원래 인수부터 다시 시작한다. 각 쌍은 `00419150`의 live nonzero team-ID 비교를 pointer identity보다 먼저 수행하며, 둘 다 실패한 뒤에만 오른쪽 live owner `+508`을 읽고 inner chain이 끝난 뒤에만 왼쪽 live owner를 읽는다. 성공은 정확히 1이고 후속 owner를 읽지 않으며, 실패는 정확히 0이다. match/team이 없는 cycle은 원본처럼 종료를 보장하지 않는다. 기존 native `UnitsHaveSameTeam4EC520`와 두 native pointer·32비트 `C.int` CGo export가 이 계약에 맞음을 재감사했고, `Object size/TeamVal/ObjOwner=780/48/508`(32비트)·`928/52/552`(64비트), `ObjectTeam size/ID=8/4`다. 전체 오라클 집계는 코드 271개·데이터 149개이고 다음 순차 원본 감사 함수는 이미 독립 봉인된 `nox_xxx_findParentChainPlayer_4EC580`이다. 기존 플랫폼별 전체 제품 차단점은 별도 widening 대상이다. 위 문단의 마지막 owner-chain identity 다음 함수와 누적 집계 표기는 이 갱신으로 대체한다.

최신 순차 범위의 object net-code search 본체 `004ECCB0..004ECD8E` 223바이트와 NOP `004ECD8F` 1바이트는 각각 SHA-256 `0c35d3a8e9c693d32c87040fbb564a7a0932f7e569a32d64a48815b7dd23621f`, `9e076ceaf246b6003d9c2680a2b4cf0bffd069805902b0b5edeebf49039fe4bd`로 봉인했다. 결합 224바이트는 `dc38c39c0bb93f51dcd9a256189ae1b3c7f68b9c0a0b383fa6a761b87fb829c7`이고 decoded direct caller는 스무 곳이며 direct jump·저장 절대 포인터는 없다. cache hit 우선, active object와 각 inventory, pending/uninitialized, active player의 정확한 검색 순서 및 dead bit, full NetCode, cache 삽입 여부와 PlayerUnit live 재로드를 계약으로 고정했다. 누적 집계는 코드 299개·데이터 156개이고 다음 순차 원본 감사 함수는 cache lookup `004ECD90`이다. 위의 오래된 중간 집계와 다음 주소는 각 시점 기록이며 이 문단이 최신 상태를 나타낸다.

최신 순차 범위의 net-code cache lookup 본체 `004ECD90..004ECDDB` 76바이트와 NOP `004ECDDC..004ECDDF` 4바이트는 각각 SHA-256 `a63883dfcb62a26521434c6fe25cf89e1ea34b0157e7c55b638aa5cca9212ec8`, `e61d6a793b42951d4e466a18683567c9011cd840b03559c0cc9e94c761995098`로 봉인했다. 결합 80바이트는 `0a2586701c638a974b95ce86f6882e4993885d262d36e02aa3319b891134d4f1`이고 sole decoded direct caller는 `004ECCB9`이며 direct jump·저장 절대 포인터는 없다. init flag·조건부 초기화·used head, entry object와 full uint32 NetCode, mismatch-only next, hit의 remove→prepend→live object reload 순서를 계약으로 고정했다. 원본 entry 배치 `12/0/4/8`과 native 64비트 `24/0/8/16`을 분리하고 raw pointer-int C 본체를 퇴역시켰다. 누적 집계는 코드 301개·데이터 156개이고 다음 순차 원본 감사 함수는 used-list prepend `004ECDE0`이다. 위의 오래된 중간 집계와 다음 주소는 각 시점 기록이며 이 문단이 최신 상태를 나타낸다.

최신 순차 범위의 net-code cache used-list prepend 본체 `004ECDE0..004ECE05` 38바이트와 NOP `004ECE06..004ECE0F` 10바이트는 각각 SHA-256 `c08382ad1de5581492aac579a79c05187e472220f10f657b56b5bc5d12d186b1`, `bde559b24d3a5302d82a4e56eb6f4b12d39057d100fd0ca81b337f5c1aa80cba`로 봉인했다. 결합 48바이트는 `9262aee22629b47e2529667454189481776d64358f465ff7d0d0bc45137b5fa9`이고 decoded direct caller는 `004ECDD0/004ECE77/004ECEC7/004ECEDD/004ECFD6/004ED009` 여섯 곳이며 direct jump·저장 절대 포인터는 없다. entry prev nil, first snapshot의 entry next 저장, 독립적인 live first 재로드, nonempty head prev 또는 empty last, 마지막 first 갱신과 entry 반환 순서를 계약으로 고정했다. 원본 entry/list 배치 `12/0/4/8`, `8/0/4`와 native 64비트 `24/0/8/16`, `16/0/8`을 분리했다. 누적 집계는 코드 303개·데이터 156개이고 다음 순차 원본 감사 함수는 used-list remove `004ECE10`이다. 위의 오래된 중간 집계와 다음 주소는 각 시점 기록이며 이 문단이 최신 상태를 나타낸다.

최신 순차 범위의 net-code cache list remove 본체 `004ECE10..004ECE42` 51바이트와 NOP `004ECE43..004ECE4F` 13바이트는 각각 SHA-256 `912bf1c41a140cb32f61b64a975233e4533da79065a8256ffc94f80f85a2f15d`, `aff312c80e826834eed3e424180d0b1150cd49ab4454e19d6d9cd884a2178915`로 봉인했다. 결합 64바이트는 `a3854e5f8f0239f6e5f3abb0a5d6b517b338f19c292b8bf5b025a60efe63ba81`이고 decoded direct caller는 `004ECDC5/004ECEBC/004ECFCB/004ECFFE` 네 곳이며 direct jump·저장 절대 포인터는 없다. 첫 next/prev 관찰에 따른 successor.prev 또는 list.last, 독립적인 두 번째 prev/next 관찰에 따른 predecessor.next 또는 list.first, 분기별 반환값과 제거 entry 링크 유지까지 계약으로 고정했다. 원본 entry/list 배치 `12/0/4/8`, `8/0/4`와 native 64비트 `24/0/8/16`, `16/0/8`을 분리했다. 누적 집계는 코드 305개·데이터 156개이고 다음 순차 원본 감사 함수는 cache initializer `004ECE50`이다. 위의 오래된 중간 집계와 다음 주소는 각 시점 기록이며 이 문단이 최신 상태를 나타낸다.

최신 순차 범위의 net-code cache initializer 본체 `004ECE50..004ECE92` 67바이트와 NOP `004ECE93..004ECE9F` 13바이트는 각각 SHA-256 `e298438350b752b3d0074a0c364c5b3d778edfa459529f5a3eeccf1e10fdc182`, `aff312c80e826834eed3e424180d0b1150cd49ab4454e19d6d9cd884a2178915`로 봉인했다. 결합 80바이트는 `751e4b941bd2c9de16356d6baf3924cd365ce0b9ac5387128bf0608f55684952`이고 sole decoded direct caller는 Go-owned lookup의 `004ECD99`이며 direct jump·저장 절대 포인터는 없다. used first/last→free first/last zero store, 16개 entry의 주소 오름차순 prepend, 마지막 needs-init clear와 최종 prepend 반환을 계약으로 고정하고 raw ABI32 initializer를 실행 경계에서 퇴역시켰다. 누적 집계는 코드 307개·데이터 156개이고 다음 순차 원본 감사 함수는 cache insertion `004ECEA0`이다. 위의 오래된 중간 집계와 다음 주소는 각 시점 기록이며 이 문단이 최신 상태를 나타낸다.

최신 순차 범위의 net-code cache insertion 본체 `004ECEA0..004ECEE5` 70바이트와 NOP `004ECEE6..004ECEEF` 10바이트는 각각 SHA-256 `31a81a54d68fe92b36ca81610afda89f1075b297e9181403175f91303d2df585`, `bde559b24d3a5302d82a4e56eb6f4b12d39057d100fd0ca81b337f5c1aa80cba`로 봉인했다. 결합 80바이트는 `fac7a080a0948a82d3eeb0f962e21fa36a5b86312ecb0c27ba04586b6676e675`이고 decoded direct caller는 Go-owned object search의 `004ECD34/004ECD44` 두 곳이며 direct jump·저장 절대 포인터는 없다. nextUnused 뒤 객체 load, free entry의 store→prepend, full cache의 single tail load→store→remove→같은 cached entry prepend와 두 경로의 prepend 결과 반환을 계약으로 고정하고 raw ABI32 add 본체를 실행 경계에서 퇴역시켰다. 누적 집계는 코드 309개·데이터 156개이고 다음 순차 원본 감사 함수는 free-entry pop helper `004ECEF0`이다. 위의 오래된 중간 집계와 다음 주소는 각 시점 기록이며 이 문단이 최신 상태를 나타낸다.

최신 순차 범위의 net-code cache free-entry pop 본체 `004ECEF0..004ECF05` 22바이트와 NOP `004ECF06..004ECF0F` 10바이트는 각각 SHA-256 `ee9cd7c59823c7740e39e19c7c6ad03e5c6dc023386a8c67d7b177ec87f8cf9d`, `bde559b24d3a5302d82a4e56eb6f4b12d39057d100fd0ca81b337f5c1aa80cba`로 봉인했다. 결합 32바이트는 `0e43d6cec8db010d154e4cd1ba85b759e10cdf3fa0aa4bd5562943fe3b175410`이고 sole decoded direct caller는 Go-owned insertion의 `004ECEA0`이며 direct jump·저장 절대 포인터는 없다. free head single load, nil 무효과, non-nil의 cached head next→free first store→original head 반환, free last와 entry 링크·object 보존을 계약으로 고정하고 raw ABI32 pop 본체를 실행 경계에서 퇴역시켰다. 누적 집계는 코드 311개·데이터 156개이고 다음 순차 원본 감사 함수는 object ScriptID lookup `004ECF10`이다. 위의 오래된 중간 집계와 다음 주소는 각 시점 기록이며 이 문단이 최신 상태를 나타낸다.

최신 순차 범위의 object ScriptID lookup `004ECF10..004ECF9F` 144바이트는 SHA-256 `5eb2f3f10c41d3ca899d6f0463693a1a7f5ab1e1c7d25d0e7b6f223f7912b0d8`로 봉인했다. 다음 함수 `004ECFA0`이 바로 이어지므로 별도 padding은 없다. decoded direct rel32 caller는 `004DBB17/004DBB45/004DBB6D/004DBB91/004DBBB5/004DBBD8/00516FCF/00516FDA/0052BB5F` 아홉 곳이고 direct jump·저장 절대 포인터는 없다. active head를 인수보다 먼저 읽고 signed 32-bit ScriptID를 한 번 cache한 뒤 active top-level→각 active inventory→pending→missile 순서로 순회하며, 모든 후보에서 flags low byte를 먼저 읽어 dead bit `0x20`이면 ScriptID를 읽지 않고 건너뛴다. 일치 시 next나 뒤 domain을 읽지 않고 동일 객체를 반환하는 계약을 native pointer-width 목록에 결속했고, raw ABI32 C 본체는 provenance-only로 퇴역시키되 아홉 C caller를 위한 typed CGo export `sub_4ECF10(int32_t)`은 유지한다. 누적 집계는 코드 312개·데이터 156개이고 다음 순차 원본 감사 함수는 net-code cache remove `004ECFA0`이다. 위의 오래된 중간 집계와 다음 주소는 각 시점 기록이며 이 문단이 최신 상태를 나타낸다.

최신 순차 범위의 net-code cache object remove `004ECFA0..004ECFDF` 64바이트는 SHA-256 `30da81608207c56fff706a01aad893d46a44ddcb2a1f85d74047dab35aa84122`로 봉인했다. 다음 함수 `004ECFE0`이 바로 이어지므로 별도 padding은 없다. sole decoded direct rel32 caller는 `004DAE30`이고 5바이트 call instruction `e8 6b 21 01 00`의 SHA-256은 `6be5bef62e235346da8538ea3fc9f8017878fb228a78090de54640860daa423f`이며 direct jump·저장 little-endian 절대 함수 주소는 없다. full 32-bit needs-init 선행 load와 raw flag 반환, initialized-empty의 zero 반환, nonempty 뒤에만 객체 인수를 한 번 읽는 순서, 각 entry의 object-before-next 비교, miss의 동일 객체 반환, hit의 exact entry used-remove→free-prepend와 stale object 보존을 계약으로 고정했다. 원본의 서로 다른 EAX 도메인은 `uint32`·native object pointer·native entry pointer tagged result로 분리했고 raw ABI32 C 본체와 prototype은 퇴역했다. 누적 집계는 코드 313개·데이터 156개이고 다음 순차 원본 감사 함수는 net-code cache clear `004ECFE0`이다. 위의 오래된 중간 집계와 다음 주소는 각 시점 기록이며 이 문단이 최신 상태를 나타낸다.

최신 순차 범위의 net-code cache clear 본체 `004ECFE0..004ED019` 58바이트와 정렬 NOP `004ED01A..004ED01F` 6바이트는 각각 SHA-256 `dd39e71c3d5366b5050118fe6c04e2fdeb779d16b4e213d118fa42e81be36930`, `ff35ffe14925642da6f3a258b35811e08101c03f8b5db346e5afcca448677564`로 봉인했고 결합 해시는 `2876a41b84c4dd619053e7df2e425583561241de4c3b698f64dcebe881a4eeca`다. 다음 함수는 object Extent lookup `004ED020`이다. sole decoded direct rel32 caller는 `004D14BB`이고 5바이트 call instruction `e8 20 bb 01 00`의 SHA-256은 `861f58b62f26eb80b7097d11987fda48a0689d0ba4445ec4645650f44ad52887`이며 direct jump·저장 little-endian 절대 함수 주소는 없다. full 32-bit needs-init 선행 load, used head single load, 각 entry의 next-before-remove, exact used-remove→free-prepend와 callback 뒤 cached-next 순회를 고정했고 entry object는 읽거나 지우지 않는다. 원본의 init/empty `uint32`와 nonempty entry EAX 도메인을 tagged result로 분리했으며 sole caller가 반환을 즉시 덮어쓰므로 public Server 경계는 이를 버린다. raw ABI32 C 본체와 prototype은 퇴역했다. 누적 집계는 코드 315개·데이터 156개이고 다음 순차 원본 감사 함수는 object Extent lookup `004ED020`이다. 위의 오래된 중간 집계와 다음 주소는 각 시점 기록이며 이 문단이 최신 상태를 나타낸다.

최신 순차 범위의 object Extent lookup 본체 `004ED020..004ED049` 42바이트와 정렬 NOP `004ED04A..004ED04F` 6바이트는 각각 SHA-256 `125abb15884b831488569e31591aa6268f6aa1d3efe53482b04495d1e01ed76f`, `ff35ffe14925642da6f3a258b35811e08101c03f8b5db346e5afcca448677564`로 봉인했고 결합 해시는 `ae35f2166614b02b320729ebb1f02063cdafabf2b086485bf393bed3100055ee`다. decoded direct rel32 caller는 17곳이고 direct jump·저장 little-endian 절대 함수 주소는 없다. active head를 인수보다 먼저 요청하며 empty면 인수를 읽지 않고, 각 후보의 flags low byte를 먼저 읽어 destroyed `0x20`이면 Extent를 건너뛴다. live 후보의 full unsigned 32-bit Extent가 일치하면 next를 요청하지 않고 동일 native 객체를 반환한다. 이 계약을 pointer-width-safe 목록에 결속하고 기존 `int` API의 low 32-bit 의미도 통일했다. raw ABI32 C 본체는 provenance-only로 퇴역시키되 source에 남은 세 C caller를 위한 `uint32_t`/native-pointer CGo export는 유지한다. 누적 집계는 코드 317개·데이터 156개이고 다음 순차 원본 감사 함수는 `004ED050`이다. 위의 오래된 중간 집계와 다음 주소는 각 시점 기록이며 이 문단이 최신 상태를 나타낸다.

최신 순차 범위의 owned Crown drop 본체 `004ED050..004ED0B6` 103바이트와 정렬 NOP `004ED0B7..004ED0BF` 9바이트는 각각 SHA-256 `60b288ea667a7c105afd0db36e630b069053180bf82033dd0459c6008d2a3917`, `f56642978961c41b24911838d549a9957c25a0dee0914c9230b5f17a3567418b`이고 결합 해시는 `042283059b72701c53e24781dc4233a3a052bc52243339ff64ad705c42ce1997`다. sole decoded direct rel32 caller는 `0054DE88`이며 call instruction SHA-256은 `97ad19e10beae4148be8458adcb0a04a9dba884260f1d740f5faeba231c5fb0e`이고 direct jump·저장 little-endian 절대 함수 주소는 없다. Crown type cache를 인수보다 먼저 읽고 zero lookup도 저장한 뒤 owned list를 순회한다. match마다 UpdateData를 drop callback 전에 cache하고 callback 뒤 cached PickupTarget store와 live next-owned load를 수행하는 계약을 native pointer-width Object/CrownUpdateData에 결속했다. sole C caller용 `void sub_4ED050(nox_object_t*,nox_object_t*)` export를 유지하고 중복 raw ABI32 본체는 provenance-only로 퇴역했다. 누적 집계는 코드 319개·데이터 156개이고 다음 순차 원본 감사 함수는 inventory insertion `004ED0C0`이다. raw `004ED5E0`과 shared-cache consumer `004ED810`은 후속 widening 범위다. 위의 오래된 중간 집계와 다음 주소는 각 시점 기록이며 이 문단이 최신 상태를 나타낸다.

최신 순차 범위의 inventory detach 본체 `004ED0C0..004ED287` 456바이트와 정렬 NOP `004ED288..004ED28F` 8바이트는 각각 SHA-256 `cad1bfba9c57f2d040f9b127645477f8aa7aceb2bed023cf6d4898f5886524f8`, `9e8376b4aa602de084708bf231f7ab5bd700e3d623bcf47a3851ce49cbe46f08`이고 결합 해시는 `ac0b99de85ebf24ba8a429a7f9eafbd5e2f5ef5795a6cf427ee9dcace3552014`다. decoded direct rel32 caller는 15곳이며 direct jump와 저장 little-endian 절대 함수 주소는 없다. cached entry class·Player UpdateData와 callback별 live Player reload, report/equip 분기, live inventory link repair, holder clear 뒤 owner clear, callback 뒤 signed wrapping weight 비교를 native pointer-width Object/PlayerUpdateData/Player 계약으로 고정했다. 남은 C caller용 `void sub_4ED0C0(nox_object_t*,nox_object_t*)` export를 유지하고 중복 raw ABI32 본체는 provenance-only로 퇴역했다. 누적 집계는 코드 321개·데이터 156개이고 다음 순차 원본 감사 함수는 default drop `004ED290`이다. service 내부와 일부 caller의 ABI32 정수 포인터는 후속 widening 범위다. 위의 오래된 중간 집계와 다음 주소는 각 시점 기록이며 이 문단이 최신 상태를 나타낸다.

최신 순차 범위의 DefaultDrop 본체 `004ED290..004ED4F7` 616바이트와 정렬 NOP `004ED4F8..004ED4FF` 8바이트는 각각 SHA-256 `7dafef68ed1787ebc26bf2c44284f8ae9605c3b04947868c42cd2cee120d17d9`, `9e8376b4aa602de084708bf231f7ab5bd700e3d623bcf47a3851ce49cbe46f08`이고 결합 해시는 `37f49011d36c8a1912dee02f460b53d1f662262aa8c1e02912d0f6749b74786e`다. 열 direct rel32 caller와 `005C9D68` registration record, `005C9DF8` NUL name을 대조했고 direct jump는 없으며 저장된 절대 함수 주소는 registration record 한 곳뿐이다. 원본 body·padding과 registration·name을 봉인한 뒤 raw C 본체는 provenance-only로 퇴역했고 C caller는 native object-pointer CGo export를 사용한다. 이 추가로 누적 집계는 코드 323개·데이터 158개가 됐다.

DefaultDrop 의존 아이템 규칙의 membership `0053EBF0..0053EC34`, initializer `0053EC40..0053EC7B`, mask query `0053EC80..0053ECD8`과 각 padding을 포함한 전체 `0053EBF0..0053ECDF` 240바이트 SHA-256은 `e33332c0c7fe299b4ecf749a307735fddd8912acde005007e8fd5eefd93d49d7`다. membership/mask/initializer direct call 일곱 곳을 대조했고 direct jump·저장 절대 함수 주소는 없다. `005CB388`의 19-row+zero-sentinel table 240바이트와 `005CB478`의 name block 300바이트 SHA-256은 `c6d72a21a4af87e6e487f25a9157770a116e30ad36413ca36fe46cbc01aaaebf`, `1ca45cb1eadb813551dc63bdcc534c992a64046c1f71eb56320d7d12601c0c1`다. 세 raw C 본문은 provenance-only로 퇴역했고 membership C caller는 native-pointer CGo export를 사용한다. 최신 누적 집계는 **코드 329개·데이터 160개**이며 다음 dependency 감사 대상은 DefaultDrop decay scheduler `00511660`이다. 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

DefaultDrop decay dependency의 setter/remove/tick/destroy와 padding 여덟 범위 `00511660..005117EF` 400바이트 SHA-256은 `7f9fd56783021264960196913f5f44ead79a4581feb1a5081e04395ee9c8847e`다. 네 body SHA-256은 `b275583a56b090da77fca0bb6a77f6d5284f38f99585b8195db3a4df42638d91`, `fd66f883bfa0b8e7a4abba1e9314a3a4507ac6d12d92303c7554087b94482b4e`, `bacaa67b65a460bac9a6897fdf4f58788986d9441cc77d0dee6898d96b21c2c2`, `f97d8e3e93cebecc645a5dae58e1eae3472158ed7c33a5825c03ea9ccd80f481`다. direct rel32 call 30곳을 대조했고 direct jump·저장 절대 entrypoint는 없다. 실제 어셈블리의 frame 1회 읽기, unsigned stable sort, remove/tick/destroy 접근 순서를 native pointer-width sidecar 계약에 결속했으며 typed setter/remove CGo export를 제외한 raw ABI32 C 본체는 provenance-only로 퇴역했다. Linux/386·Windows/386 여섯 제품에서 네 body와 400-byte pattern은 모두 0개다. 최신 누적 집계는 **코드 337개·데이터 160개**이며 보유 `nox/` 1,556개 파일·570,653,750바이트·트리 SHA-256 `161675279c5a9a6e5e8da4ae539ad80f9033d608b32ad620a052866ecc1e61b7`의 전후 동일성과 NXZ strict 50쌍을 통과했다. 다음 주소 순서 감사 대상은 GlyphDrop `004ED500`이며 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

GlyphDrop 본체 `004ED500..004ED57D` 126바이트와 NOP `004ED57E..004ED57F` 2바이트의 SHA-256은 각각 `7c9094d2b9140c78ae2ffba78b5adadaa886564bb686e23c96cf0dd4fc6608e2`, `182003d5c37dc5253d84cc5156ca9f93aab75e72e395d157748de67cc20f4f76`이고 결합 128바이트는 `832a96e51ef466910d9cf918a5b074e616e88b76bb5be82dd6a87fcccc5e7749`다. TrapDrop 본체 `004ED580..004ED5D8` 89바이트와 NOP `004ED5D9..004ED5DF` 7바이트는 각각 `468a51fcee4362c3392de9b915e6f7df0cab965d4431dc145130a699ae3c38a8`, `ca4b9a2ec05863e71b87c84feb71741348a30400daeddedd67bc4cdbca737252`이고 결합 96바이트는 `087e1e333e8c3a42d7a8e55d0767b49a364e14fbd315ae7d5e092c171735540a`다. GlyphDrop에는 decoded direct call/jump가 없고 little-endian 절대 주소 저장은 `005C9D98` registration record 한 곳뿐이다. TrapDrop의 sole direct rel32 caller는 GlyphDrop의 `004ED515`이고 instruction `e8 66 00 00 00` SHA-256은 `a57ddc1c9e65553c47ab179ff339c3a4618a315feac5f2741f88b5a9ff246718`이며 direct jump는 없고 절대 주소 저장은 `005C9DB0` record 한 곳뿐이다. 두 record와 `005C9E2C` GlyphDrop·`005C9E44` TrapDrop name block을 각각 독립 범위로 봉인했다. GlyphDrop은 TrapDrop whole-EAX gate 뒤 InitData point store, alias 가능한 live point 재로드, x87 binary32 spill 방향, Direction2→Direction1→audio 825 순서를 보존한다. TrapDrop은 map callback 전 point cache, 금지 경로의 live owner NetCode→audio 925, 허용 경로의 owner→trap load와 DefaultDrop→owner transfer를 보존한다. 두 raw ABI32 본체는 provenance-only로 퇴역했고 등록 CGo 경계와 Go dispatch는 세 native pointer를 사용한다. 타일 판정 `00411A90`과 DefaultDrop 하위 서비스 내부는 별도 복원 범위다. 최신 누적 집계는 **코드 341개·데이터 164개**이며 다음 주소 순서 감사 대상은 CrownDrop `004ED5E0`이다. 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

CrownDrop 본체 `004ED5E0..004ED705` 294바이트와 NOP `004ED706..004ED70F` 10바이트의 SHA-256은 각각 `1d0cf7df540eaa615ecafd86b0b9603d874e33b65b36f844f669d28c2caaffd4`, `bde559b24d3a5302d82a4e56eb6f4b12d39057d100fd0ca81b337f5c1aa80cba`이고 결합 304바이트는 `e66b0a9a8b3dd1131dfb60d5dd893362e6277a4a4a687a6b724810d184993dd4`다. decoded direct rel32 caller는 `004E6929`와 `004ED09D` 두 곳이며 5바이트 call instruction SHA-256은 각각 `fc99a1b80cf1c8d181fa1654cb314fa50d992154042895e06976b0c6f1e03489`, `6df0c5d503a0e66e10409606da805d3cd2c46cb348b4ff432867e770ccf7c668`다. direct jump는 없고 little-endian 절대 함수 주소 저장은 `005C9DC8` registration record의 callback 슬롯 `005C9DCC` 한 곳뿐이다. record와 `005C9E5C` CrownDrop name block도 각각 독립 범위로 봉인했다. KOTR·gameplay gate 뒤 frame을 한 번 읽고 Crown team이 있을 때만 UpdateData를 cache한다. 첫 Player callback 뒤 Crown 후보를 읽으므로 Player가 없으면 Crown 자체가 남고, 각 반복은 live Crown→cached Player UpdateData→live Crown team ID→실제 team membership 순서로 읽는다. pickup frame은 signed 32-bit strict minimum으로 비교해 동률의 앞 후보를 유지하고, 반복 뒤 Crown은 다시 읽되 PickupTarget은 처음 cache한 update에 기록한다. DefaultDrop whole-EAX 성공 뒤 Crown owner clear→owner enchant 30 해제→live NetCode→team 존재 확인→조건부 live team ID→10바이트 inform code 11→Crown minimap mark 순서를 보존한다. `nox_xxx_netInformTextMsg2_4DA180`이 payload 첫 두 바이트를 덮어쓰므로 Go packet builder는 이를 명시적으로 `MSG_INFORM/11`로 채운다. 두 원본 caller는 현재 Go 소유 `playerGoObserver_4E6860`과 `DropOwnedCrowns4ED050` 경로에서 등록 Go 함수를 직접 호출하고, typed CGo export는 등록 포인터 호환성을 위해 유지한다. 팀 membership·DefaultDrop·buff service 내부는 별도 복원 범위다. 최신 누적 집계는 **코드 343개·데이터 166개**이며 다음 주소 순서 감사 대상은 TreasureDrop `004ED710`이다. 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

TreasureDrop 본체 `004ED710..004ED787` 120바이트와 NOP `004ED788..004ED78F` 8바이트의 SHA-256은 각각 `7e9a32a0355e5f9d3baa19cfd1f68b9fa57745431f2d8487b68c4346aa5a9eaf`, `9e8376b4aa602de084708bf231f7ab5bd700e3d623bcf47a3851ce49cbe46f08`이고 결합 128바이트는 `b14c7ea7c6ff3648750bf110b9b4f3a5bab21038433019c7cf92a238707e1f2b`다. decoded direct rel32 call/jump는 없고 little-endian 절대 함수 주소 저장은 `005C9D8C` registration record의 callback 슬롯 `005C9D90` 한 곳뿐이다. record와 `005C9E1C`의 16바이트 TreasureDrop name block도 독립 범위로 봉인했다. 원본은 point→treasure→owner 순서로 인수를 읽어 DefaultDrop을 호출하고 full EAX zero에서 즉시 실패한다. 성공 뒤 owner class 저위 바이트의 Player bit와 Scavenger game flag 64를 차례로 검사하며, 해당 경로에서 owner UpdateData를 한 번 cache한다. 첫 live Player 포인터의 `Field2152`를 dword wrapping decrement하고 treasure maximum callback 뒤 cached update의 Player 포인터를 다시 읽어 `Field2156`에 저장한 다음 report→audio 308 순서로 호출한다. 구현 결속 전에도 이 경계와 registration 근거는 누적 **코드 345개·데이터 168개** 오라클로 고정하며 다음 주소 순서 감사 대상은 object drop dispatcher `004ED790`이다. 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

TreasureDrop의 직접 보고 의존성 `004D8CD0..004D8D36` 103바이트와 NOP `004D8D37..004D8D3F` 9바이트의 SHA-256은 각각 `4cf0f116a61d4e57f8a6bb4b7e3523b8207aec5009aa0e8114e6f02434904121`, `f56642978961c41b24911838d549a9957c25a0dee0914c9230b5f17a3567418b`이고 결합 112바이트는 `0c3693b3ac7d62d05d253eacdf2f42549553bfb0ff78bc5bc8cc9642aa44e5a7`다. decoded direct caller는 TreasureDrop의 `004ED765`와 TreasurePickup의 `004F35F5` 두 곳이며 call instruction SHA-256은 `da9b9c62315fa433e3e8c40574dba66be25f5d7540e080d07c217322935b90e0`, `bab634c9169b4aa5441775d81dbe6869677e581bd5b1e0acdf3bc90f1db57c1b`다. direct jump와 저장된 little-endian 절대 entrypoint는 없다. 원본은 owner class 저위 바이트 뒤 UpdateData를 cache하되 Player gate 전에 둘 다 읽는다. Player 경로는 opcode 85를 unit-code callback 전에 쓰고, cached update에서 Player 포인터를 각각 다시 읽어 `Field2152/Field2156`의 저위 16비트를 packet에 기록한 뒤 `(255, packet, 7, nil, 1)`로 전송한다. 누적 오라클은 **코드 347개·데이터 168개**이며 다음 주소 순서 감사 대상은 object drop dispatcher `004ED790`이다. 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

Object drop dispatcher 본체 `004ED790..004ED80A` 123바이트와 NOP `004ED80B..004ED80F` 5바이트의 SHA-256은 각각 `e10b9688323d79f1a247cd93f71467ee21cb5efaa224a37ef4ba37392abf9947`, `18e800921eac4b6ea289ffc28abb7e2d58e7521d3568dcacd9e3aa55096f35de`이고 결합 128바이트는 `d3c5842d5273879e866d582324c9d84b47d76d1272afaf727182c535222585a5`다. 다음 함수는 `004ED810`이다. decoded direct rel32 caller는 `004E6971`, `004EAFD8`, `004EB221`, `004ED91A`, `004ED956`, `004EDB93`, `004EDC9A`, `004EDDB8`, `004EE224`, `004EE276`, `00513BD0`, `0051CAA3`의 열두 곳이고 5바이트 call instruction SHA-256은 주소 순서대로 `d3e26242010f6ddf85131bf43776a7ec6f4dce0c67358cc363f29557b411faa9`, `0aef6dea88d27d40aff8ea1cdbe05c68f188a757e14ea4a72f432ebad75d160b`, `c6a717fa3adcf34dba999adc75017f068960c083147b87abdfb0e2ef0f99f7c1`, `e6246ab9452f3158c3aeb6257253d18d233c1e88d5dd13d34ca332c068a4c06a`, `5af8d24666c434cbe25414c6a13cb0e01cb80b4fc9bac0cc099cbfedfb9ef551`, `9d98176393e357633fef8fda6c46ba31bb1adef7abec24a94483a65cea85ad88`, `e2dc6d68b9b3cd838bc69639429d72f37831f0af928ad8cf9cc8b3b2537a4d89`, `d796d0c0a48a719f111382c7bbde586cd55d1ca22aa7ee614e005dcbcc372500`, `63d385a7cd6df4451bcb06e86cd6fba1c985c723aabcd2d45c18379bce4382ae`, `d64ecb8441a61394c038c8af8be77df46731e97c485b786bcdb8064bffbaf4dc`, `1914889e5aeb6c06e5a69a07edc958e1b1e07b2b229180194da474640aa49b19`, `80f8c91626ee8d2ac1c7003b0786249932300cdb1ead5c5af9c0f360144cbdce`다. direct jump와 저장된 little-endian 절대 entrypoint는 없다. 원본은 dropped item nil을 owner·point·game callback보다 먼저 단락한다. `0x2000` game flag의 whole EAX가 nonzero이고 `0x1000`의 whole EAX가 zero이며 item class full dword가 `0x03001010`과 겹칠 때만 live flags dword에 `0x40`을 OR하고 unit refresh를 호출한다. 그 callback 뒤 item의 `+712` Drop slot을 다시 읽어 non-nil이면 point→owner를 읽고 `(owner,item,point)`로 호출하며, nil이면 같은 인수로 DefaultDrop에 폴백한다. 두 경로 모두 whole EAX를 그대로 반환한다. 누적 오라클은 **코드 349개·데이터 168개**이며 다음 주소 순서 감사 대상은 `004ED810`이다. 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

Bounded object drop 본체 `004ED810..004ED927` 280바이트와 NOP `004ED928..004ED92F` 8바이트의 SHA-256은 각각 `b1e1d011280867da4d53b5135d765c086e8caac1cdb47adc6302775c30388fb0`, `9e8376b4aa602de084708bf231f7ab5bd700e3d623bcf47a3851ce49cbe46f08`이고 결합 288바이트는 `ecfef0da4291b9baa807ecd467b4cccf0922191ca52580092c432ce4b7905203`다. 다음 함수는 force-drop `004ED930`이다. decoded direct rel32 caller는 network drop request의 `0051BE59` 한 곳이고 5바이트 instruction `e8b219fdff`의 SHA-256은 `c7e6281213c628214966b6c8a29186d36a695e7941d30e2e2556ebc4de6343d3`다. direct jump와 저장된 little-endian 절대 entrypoint는 없다. binary32 `75.0` 상수와 `drop.c:DropNotAllowed`, `Crown` 두 padded 문자열도 독립 데이터 범위로 봉인했다. 원본은 owner X/Y와 요청 point X/Y를 local float4에 복사해 x87 ordered strict `distance > 75`일 때만 목적지를 projection하고, local origin/destination으로 trace한다. trace zero이면 priority message 뒤 live owner NetCode를 읽어 audio 925를 호출하고 canonical zero를 반환한다. trace nonzero이면 KOTR game flag 뒤 item 인수를 읽고, KOTR에서만 shared Crown cache를 lazy lookup/store한 뒤 zero-extended TypeInd와 비교해 Crown이면 zero를 반환한다. 나머지는 local destination pointer로 `004ED790`을 호출하고 whole EAX를 보존한다. 누적 오라클은 **코드 351개·데이터 171개**이며 다음 주소 순서 감사 대상은 `004ED930`이다. 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

Force-drop 본체 `004ED930..004ED962` 51바이트와 NOP `004ED963..004ED96F` 13바이트의 SHA-256은 각각 `c01533a085ba632a5a18ff549f9e9c6613fafd323d8602b2ae44325a366c58f4`, `aff312c80e826834eed3e424180d0b1150cd49ab4454e19d6d9cd884a2178915`이고 결합 64바이트는 `299771e67864b014a543ecc9014e39c39a8103c7e5c28e841651bbbe76d2773a`다. 다음 함수는 random reachable point `004ED970`이다. decoded direct rel32 caller는 `004EA6F8`, `004F8448`, `00513C39`, `0052C0E9` 네 곳이고 instruction SHA-256은 주소 순서대로 `a97627c967bd256481dc17de4b78b01b169c54aa841f4e7a4027c7bbb7f41b79`, `8eb00b7e10acc813d8b07c8e1588cacc71f70846abaee093c9eb842a1194a0f6`, `e19947817ab86a7a16659dbd4fb1e7fe0b5cdf48a7b7eff32ad7f42908ce733b`, `71be5cfff8ae11d814fbe44a8c2f437197de1c45b8ed5180abc86dabbbbcfb4d`다. 첫 호출은 이미 봉인된 Flag pickup 본체 안에 있어 중복 범위를 추가하지 않고 나머지 세 instruction만 독립 봉인했다. direct jump와 저장된 little-endian absolute entrypoint는 없다. 원본은 exact binary32 `50.0`, owner `+56` position pointer와 local output pointer를 `004ED970`에 넘기고 helper 반환 pointer는 버린다. helper가 반환한 뒤에만 item 인수를 읽고 cached owner·item·같은 local output pointer를 `004ED790`에 넘겨 whole EAX를 보존한다. 누적 오라클은 **코드 356개·데이터 171개**이며 다음 주소 순서 감사 대상은 직접 의존 `004ED970`이다. 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

Random reachable point 본체 `004ED970..004EDA3C` 205바이트와 NOP `004EDA3D..004EDA3F` 3바이트의 SHA-256은 각각 `565cd3f3cc16f151c1fc05ca3b68d2fe4fa2526f7d6645c6224292db2cb8b168`, `e65ca7c06ae3e9bacd16f6d87026d2fd51447f87f8771676568af93c6313d707`이고 결합 208바이트는 `8818b275081d53bf786179e635f8292217aff1a5f95608600d19f3f181afe8e2`다. 다음 함수는 drop-all-items `004EDA40`이다. decoded direct rel32 caller는 `00419317`, `004D1B16`, `004D1EC9`, `004E750C`, `004E826E`, `004ED946`, `004EDD45`, `004EDDAC`, `004F20CB`, `004F80E5`, `00513BC4`, `00530424`, `005380F4`, `00549EEE`, `0054A035`, `0054A3D5`, `0054A508`, `0054A637`, `0054E525`의 19곳이다. 이 중 `004E750C`, `004E826E`, `004ED946`은 이미 봉인된 함수 본체에 포함돼 나머지 16개 instruction만 독립 범위로 추가했으며 direct jump와 저장된 little-endian absolute entrypoint는 없다. `00583C28`의 exact binary32 increment `0x3ff1463b`와 step factor `0x3c800000`, `005B9404`의 NUL 종료 debug path도 별도 데이터 범위로 봉인했다. 원본은 center X/Y를 ray origin으로 RNG 전에 한 번 cache하고 exact binary32 `[-pi,+pi]`로 RNG를 한 번 호출한다. 각 시도에서는 저장된 angle과 increment의 x87 합을 cosine에 먼저 사용한 뒤 그 합을 binary32로 spill해 다음 angle과 sine 입력으로 쓰며, radius와 center X/Y는 후보 계산 시 live로 읽는다. trace에는 local origin/destination, nil 두 개, flag `1`을 전달하고 whole EAX nonzero에서 local destination을 X→Y 순서로 output에 복사한다. 실패마다 step을 뺀 radius를 binary32로 spill하며 64번째 실패 뒤에도 이 감소를 수행한 다음 live center를 X→Y 순서로 output에 복사한다. 두 경로 모두 정확히 output pointer를 반환한다. 누적 오라클은 **코드 374개·데이터 173개**이며 다음 주소 순서 감사 대상은 drop-all-items `004EDA40`이다. 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

직접 logic RNG 의존성 `00416030..00416080` 81바이트와 NOP `00416081..0041608F` 15바이트의 SHA-256은 각각 `8390d5e344f9ee688a501ebc53f7ee788e6ee076a34acff7224f9271c6aa15b1`, `40f0d021fa824f3b40dc646f67479997734d273d9121690b6f042c512df3a838`이고 결합 96바이트는 `9ecbfbe9ceac05eb0a4e01e9b46a791d0e5c73f81fcbf999d956749860b8e4ba`다. decoded direct rel32 caller는 41곳이고 `004ED9A8` 호출은 이미 random reachable point 본체에 포함되며 direct jump와 저장된 little-endian absolute entrypoint는 없다. 기존 `0058307C` positive-zero 범위에 RNG range 비교 용도를 추가하고, `00583080`의 exact binary32 scale `0x38000100`과 `00590404..00594403`의 4,096개 little-endian int32 표를 SHA-256 `7dfccdce34d74e348fae4181630a4c41a20a560432658ba2526969bb7000605f`로 봉인했다. 원본은 max-min을 x87에 유지하고 zero 또는 unordered range에서는 표 index를 전혀 갱신하지 않은 채 max를 반환한다. 정상 경로는 old logic index를 cache하고 wrapping 4,096 증가를 먼저 저장한 뒤 table dword를 signed integer로 적재해 binary32 scale, unspilled range, min 순서로 계산하고 x87 값을 그대로 반환한다. BSS logic index `0062EFD0`은 file-backed 데이터가 아니므로 해시 범위를 만들지 않았다. Go `github.com/opennox/libs/prand` 표를 4,096개 uint32 little-endian으로 직렬화한 SHA-256도 원본 표와 동일하고 최댓값은 `0x7FFF`다. 누적 오라클은 **코드 376개·데이터 175개**이며 다음 주소 순서 감사 대상은 계속 drop-all-items `004EDA40`이다. 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

Drop-all-items 본체 `004EDA40..004EDCB1` 626바이트, `004EDCB2..004EDCB3`의 2바이트 `mov edi,edi` 정렬, `004EDCB4..004EDCC3`의 16바이트 방향 dispatch table, `004EDCC4..004EDCCF`의 12바이트 NOP SHA-256은 각각 `a797f53fbe442bf312df0a948b959a29779a5b28b3843f8e5fba6f6025157e72`, `a904529e1b089420ec3656cc6483691fb43d23dd29a47f2fb7602832df5f2489`, `d42159cafbd65fca272dc6985f91117d7398d30c84edab8940270673167edc91`, `ab16a4264a14a2fd326c262e20ab7a8d0e67bc1658371fe45c446f311cdb6dbd`다. 전체 `004EDA40..004EDCCF` 656바이트 SHA-256은 `e16976423e31536a17dfae682910bbbc9b055d0631b652e6361f72190ed21504`이고 다음 함수는 item drop predicate `004EDCD0`이다. decoded direct rel32 caller는 `004D2754`, `004DE746`, `004E5E9E`, `004E9D61`, `0050A524`, `0052C092`, `0054D639` 일곱 곳이다. `004E5E9E`와 `004E9D61`은 이미 봉인된 함수 본체에 포함돼 나머지 다섯 instruction만 독립 범위로 추가했다. direct jump와 저장된 little-endian absolute entrypoint는 없고 dispatch table의 네 dword는 내부 case `004EDC36/3D/40/47`만 가리킨다. `005839BC` exact binary32 `6.0`, `005B9434/005B9464`의 동일한 NUL 종료 `C:\NoxPost\src\Server\Object\pickdrop\drop.c`와 source line 823/824도 독립 데이터로 봉인했다. 원본은 inventory를 한 번 훑어 predicate가 참인 item radius의 ordered strict maximum을 구하고, head와 owner position을 다시 읽어 `2*max+6` binary32 간격과 고정 origin ray를 만든다. 두 exact `[-3,+3]` RNG를 사용하는 candidate마다 live owner X/Y에서 square-perimeter point를 만들고 whole EAX trace가 nonzero이면 cached current item을 drop dispatcher에 넘긴다. item successor는 predicate·trace·drop보다 먼저 cache하고, 성공 여부와 무관하게 drop 반환을 버리며, 성공한 candidate 뒤에도 spiral state를 한 칸 전진한다. 한 perimeter에서 하나라도 성공하면 더 큰 홀수 perimeter로 확장할 수 있지만 하나도 성공하지 못한 perimeter에서는 현재 item부터 남은 cached-successor suffix를 live owner position으로 drop하는 fallback으로 전환한다. 구현 결속 전 경계 오라클은 **코드 385개·데이터 178개**이며 다음 직접 의존성 감사 대상은 item drop predicate `004EDCD0`이다. 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

Item drop predicate 본체 `004EDCD0..004EDCF4` 37바이트와 NOP `004EDCF5..004EDCFF` 11바이트의 SHA-256은 각각 `9a2cb2a021d3b405de9bd2a4ef5edd74e0f0fe36a42950cd80a81ec44884882b`, `19f3c2045194c5d2e45451e3dfe6a203b5e240aec5a2400a92cdb425c3331137`이고 결합 48바이트는 `0135181a4754242d47575d2781d9eddaba172c49555562493d7e04009d1c7e8e`다. 다음 함수는 `004EDD00`이다. direct caller 세 곳 `004EDA61/004EDAF2/004EDC88`은 모두 이미 봉인된 drop-all-items 본체 안에 있고 direct jump와 저장된 little-endian absolute entrypoint는 없다. 원본은 item 포인터와 full flags dword를 owner보다 먼저 읽는다. 저위 Destroyed `0x20`이면 owner를 읽지 않고 canonical one을 반환한다. 그렇지 않으면 owner class 저위 byte의 Player/Monster mask `0x06`을 읽어 둘 다 없을 때 canonical one을 반환하고, unit owner이면 cached item flags의 `0x10000000`이 없을 때 one, 있을 때만 canonical zero를 반환한다. nil item은 owner보다 먼저 fault하고 nil owner는 non-Destroyed item에서만 fault할 수 있는 정확한 prefix다. 누적 오라클은 **코드 387개·데이터 178개**이며 다음 단계는 `004EDA40/004EDCD0` 의미 계약과 native 결속이다. 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

Class-selected inventory respawn 본체 `004EDD00..004EDD6E` 111바이트와 NOP `004EDD6F` 1바이트의 SHA-256은 각각 `01790d3a4b089fda2a7311b99106f7ac9b19b9468a911574ae39f8e27b6ae81c`, `9e076ceaf246b6003d9c2680a2b4cf0bffd069805902b0b5edeebf49039fe4bd`이고 결합 112바이트는 `05a08883918d9551129e5bef516b7dfb7428d4d7795301cb468cc623c56cefbb`다. player flag-class inventory drop 본체 `004EDD70..004EDDDC` 109바이트와 NOP `004EDDDD..004EDDDF` 3바이트의 SHA-256은 각각 `5990c3d872f708ad7c1b7d7e0d12b6e22b3154799c0a23a556dc5f84a73af39f`, `e65ca7c06ae3e9bacd16f6d87026d2fd51447f87f8771676568af93c6313d707`이고 결합 112바이트는 `455034ce93c0bf3bea3abbe46e97fe330e98e9b6c42a9fa375c925f25672a0c1`다. 두 함수와 패딩을 합친 `004EDD00..004EDDDF` 224바이트 SHA-256은 `01c575aac8a88a3a4b7890a440cc573215af0bac38525b6b377771d17a9082a0`이고 다음 함수는 PotionDrop `004EDDE0`이다. decoded direct rel32 caller는 `004EDD00`에 `004D73A9/004D741C` 두 곳, `004EDD70`에 `004D2877` 한 곳이며 각 5바이트 instruction SHA-256은 주소 순서대로 `80eea45057fcbe150466d897148dc9862b6e4ca44321a3084b1d984efb6005fe`, `dbcfa05fe7bdce2ca984bf2d2c357e57b90fe56f1db76d67c6b94db595460c58`, `277a7e3b53d0c0585e03f925d1a5d1eec629640f57b95ff153f5c9aa75998a7d`다. direct jump와 정렬된 little-endian absolute entrypoint는 없다. raw `00 dd 4e 00`은 `.data`의 비정렬 file offset `0x00191AF7`에서 인접 dword 경계를 가로질러 한 번 나타나므로 함수 포인터가 아니다. 새 본문 두 개가 기존 random-helper caller 범위 `004EDD45/004EDDAC`를 포함하므로 그 두 독립 레코드는 중복 없이 본문 레코드로 대체했다. `004EDD00`은 inventory head를 먼저 읽고, 각 item successor를 class보다 먼저 cache하며 full class dword와 caller mask의 겹침을 검사한다. 일치하면 detach 뒤 exact live owner position에 radius `60.0` random helper를 호출하고 반환값을 버린 다음 local point의 X/Y를 읽어 nil owner로 item을 다시 생성한다. `004EDD70`은 첫 player unit부터 시작해 direct inventory head를 읽고, item class 뒤 successor를 cache한 다음 class bit `0x10000000` 일치마다 exact live player position과 radius `50.0`으로 local point를 만들고 drop 반환값을 버린다. player successor는 해당 inventory scan을 마친 뒤 live로 읽는다. 누적 오라클은 **코드 392개·데이터 178개**이며 다음 단계는 두 함수의 의미 계약과 native 결속이다. 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

구현 결속은 `5fb2771c3/dc5e8f787/8561febaf/e0404e7a7/2ba73404a`로 오라클·순수 의미·native server·Go 호출 경로·C ABI를 분리했다. 원본의 successor/class 읽기 순서, full class dword, exact live position, radius `60.0/50.0`, helper/drop 반환 무시, local point identity, next-player 지연 읽기와 fault prefix를 반복·race/checkptr 시험으로 고정했다. 유효 아홉 Go tuple, 아홉 C frontend와 추가 Windows/ARMv7, Darwin/Windows 다섯 생성 CGo target, 실제 Linux/386 실행 및 Windows/386 PE32 링크를 통과했다. 여섯 전체 산출물에는 위 111/112/109/112/224바이트 원본 pattern이 모두 0건이고 legacy가 링크된 네 제품에만 exact public `sub_4EDD70`가 하나씩 있다. `make oracle-test`는 구현 뒤에도 코드 392개·데이터 178개와 원본 트리 전후 동일성을 재확인했다. 다음 순차 감사 대상은 PotionDrop `004EDDE0`이다.

PotionDrop 본체 `004EDDE0..004EDE4C` 109바이트와 NOP `004EDE4D..004EDE4F` 3바이트의 SHA-256은 각각 `08d56d64b0a0d183bc35e1c8cb009bfbc7a49eaf9538438a0b3ee2fe893e0294`, `e65ca7c06ae3e9bacd16f6d87026d2fd51447f87f8771676568af93c6313d707`이고 결합 112바이트는 `927424648139da3934fee15b7fa0ca729c7bfdda85bc2f66f47d32a76cda8d5d`다. 다음 함수는 FoodDrop `004EDE50`이다. decoded direct rel32 call/jump는 없으며 little-endian absolute entrypoint는 정렬된 `.data` file offset `0x001C9DA8`, VA `0x005C9DA8`에 정확히 한 번 존재한다. 이를 포함하는 `005C9DA4`의 12바이트 등록 레코드는 이름 포인터 `005C9E38`, 콜백 `004EDDE0`, null parser이고 SHA-256은 `6f41b2d9ef911dce7b85741fb6dfc64a009cce796341bb6f5595cc3c8bd29f01`이다. `005C9E38`의 `PotionDrop\0` 및 정렬 12바이트 SHA-256은 `8321cf1a9ffeb264319fab0be077c48b03241ae4a1abcae8376f8c40dfcc32b6`다. 원본은 point→owner→item 인자 값을 먼저 적재하고 DefaultDrop을 호출한다. whole EAX가 zero이면 즉시 canonical zero를 반환한다. 성공하면 cached item으로 audio 833을 먼저 발생시킨 뒤 flag `0x800`, 필요할 때만 `0x1000`을 순서대로 검사한다. 둘 다 whole EAX zero일 때만 live `gameFPS`를 한 번 읽고 32비트 wrapping `25*FPS`를 decay setter에 전달한다. audio와 decay 반환은 무시하며 모든 성공 분기는 canonical one이다. 누적 오라클은 **코드 394개·데이터 180개**이며 다음 단계는 PotionDrop의 의미 계약과 native 결속이다. 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

구현 결속은 `d3f83d6e6/82a2e931c/7f13c47c0/4b97e7065/417c1d6b2`로 오라클·순수 의미·native server·Go 등록 경로·C ABI를 분리했다. point→owner→item 선적재와 cache, DefaultDrop whole-int32 gate, audio 833 선행, `0x800` 뒤 조건부 `0x1000`, 두 flag가 모두 zero일 때만 수행되는 live FPS 단일 읽기와 wrapping `25*FPS`, canonical 0/1과 fault prefix를 반복·race/checkptr 시험으로 고정했다. 유효 아홉 Go 1.26.5 계약 tuple, Darwin 두 ISA 실행, 독립 C frontend와 다섯 생성 CGo target, 실제 Linux/386 실행 및 Windows/386 PE32 링크를 통과했다. 여섯 전체 산출물에는 위 109/112바이트 원본 pattern이 모두 0건이고 legacy가 링크된 네 제품에만 exact public `sub_4EDDE0`가 하나씩 있다. `make oracle-test`는 구현 뒤에도 코드 394개·데이터 180개와 원본 트리 전후 동일성을 재확인했다. 다음 순차 감사 대상은 FoodDrop `004EDE50`이다.

FoodDrop 본체 `004EDE50..004EDEFC` 173바이트와 NOP `004EDEFD..004EDEFF` 3바이트의 SHA-256은 각각 `1ab18a2b4d7458741753a793038ca6c892b666ff1ab5a13ff577c781ab2fa94a`, `e65ca7c06ae3e9bacd16f6d87026d2fd51447f87f8771676568af93c6313d707`이고 결합 176바이트는 `665623f4ec2b763b60daef90e308335febc7fd97d8368454de571a9e522c1c6f`다. 다음 함수는 chest routine `004EDF00`이다. decoded direct rel32 call/jump는 없으며 little-endian absolute entrypoint는 등록 callback cell인 정렬된 `.data` file offset `0x001C9DC0`, VA `0x005C9DC0`에 정확히 한 번 존재한다. 이를 포함하는 `005C9DBC`의 12바이트 등록 레코드는 이름 포인터 `005C9E50`, callback `004EDE50`, null parser이고 SHA-256은 `79c7f1efd20594b170563ce3914d0c79f1f272de3030e5857028f913f3c797f4`다. `005C9E50`의 `FoodDrop\0` 및 정렬 12바이트 SHA-256은 `170c91531f9078e34a8ded5ab87ab65c090aa3da58d8c9c68d74bfebe491ecc4`다. `005B9388`의 40바이트 sound-rule table SHA-256은 `3efc321619f08f5bae22a4f547921be21e03901d5b2472f0eb9a441cf3e4c5f2`이며 `{subclass mask, low flags mask, sound}`는 순서대로 `{0,1,835}`, `{2,0,837}`, `{4,0,833}`, `{0x80,0,839}`, `{0,0,0}` sentinel이다. 원본은 owner→food→point를 각각 읽고 nil이면 즉시 canonical zero를 반환한 뒤 세 값을 cache해 DefaultDrop을 호출한다. whole EAX zero이면 그대로 반환하고, nonzero result는 끝까지 cache해 비정규 값을 포함한 exact 반환을 보존한다. 성공 뒤 game flag `0x800` whole EAX가 zero일 때만 live `gameFPS`를 한 번 읽어 32비트 wrapping `25*FPS` decay를 cached food에 설정한다. 그 다음 첫 rule의 sound word가 zero인지 확인하고, 각 row에서 cached food의 full subclass dword를 먼저 live로 읽어 mask와 검사하며 불일치할 때만 row의 low-flags mask와 food flags 저위 word를 검사한다. match는 sound를 zero-extend해 cached owner에 audio를 한 번 발생시키고, sentinel까지 불일치하면 audio 없이 cached DefaultDrop result를 반환한다. decay와 audio 반환은 무시한다. 누적 오라클은 **코드 396개·데이터 183개**이며 다음 단계는 FoodDrop의 의미 계약과 native 결속이다. 위의 모든 이전 집계와 다음 주소 표기는 이 문단이 대체한다.

구현 결속은 `b0c7e611f/4893464f5/9f8baabf0/94631286b/e6cd1e454`로 오라클·순수 의미·native server·Go 등록 경로·C ABI를 분리했다. owner→food→point nil guard와 cache, DefaultDrop whole-int32 gate와 exact nonzero 반환, `0x800` zero에서만 수행되는 live FPS 단일 읽기와 wrapping decay, subclass-first/low-flags-second sound table 순회와 sentinel·fault prefix를 반복·race/checkptr 시험으로 고정했다. raw C 본체는 provenance-only이고 exact public CGo 경계는 `int nox_xxx_dropFood_4EDE50(nox_object_t*, nox_object_t*, float2*)`다. 유효 아홉 Go 1.26.5 계약 tuple, Darwin 두 ISA 실행, 독립 다중 ISA C frontend와 다섯 생성 CGo target, 실제 Linux/386 실행 및 Windows/386 PE32 링크를 통과했다. 여섯 전체 산출물에는 위 173/176바이트 원본 pattern이 모두 0건이고 legacy가 링크된 네 제품에만 ABI 정규화 기준 exact public 심볼이 하나씩 있다. `make oracle-test`는 구현 뒤에도 코드 396개·데이터 183개와 원본 트리 전후 동일성을 재확인했다. FoodDrop을 20개 포팅 단위당 한 번 수행하는 전체 9-tuple 행렬의 새 기준점 `1/20`으로 삼고 다음 19개 단위는 macOS 상시 검증만 수행한다. 다음 순차 감사 대상은 chest routine `004EDF00`이다.

Chest inventory-drop 본체 `004EDF00..004EE294` 917바이트와 NOP `004EE295..004EE29F` 11바이트의 SHA-256은 각각 `696bc6a4df8103fd97c10f8618566d4290754ae2297178c14fc39d1d4d961094`, `19f3c2045194c5d2e45451e3dfe6a203b5e240aec5a2400a92cdb425c3331137`이고 결합 928바이트는 `edd2e358a6e56fa5a50f642b4b15a215d93b3a67599ca0074947e9b4d9a9046d`다. 직접 shape-extent helper `004EE2A0..004EE2EE` 79바이트와 NOP `004EE2EF` 1바이트의 SHA-256은 각각 `6b3ada732f00dc9e77b3cf94b2302e90cb3aa11caeea116ef425823c18c2cc96`, `9e076ceaf246b6003d9c2680a2b4cf0bffd069805902b0b5edeebf49039fe4bd`이고 결합 80바이트는 `8c6af9303c1442df78bd5005b1ea7e775e056de8ddf86d100bf3afc8a813f2a8`다. chest의 sole direct caller는 이미 봉인된 ChestCollide 안의 `004E9D5B`, helper의 sole direct caller는 새 chest 본체 안의 `004EDFB7`이며 두 5바이트 call instruction SHA-256은 `657c0ced5374b3351ccc3f11fb5f590ce3cf30c5d854b041a0b8da88dd2580b1`, `8516dc9292d4485fe1c9e7b6008f1141d0da5ccad5f2451bb2c8228f519730c7`다. 두 entrypoint 모두 direct jump와 저장된 little-endian absolute pointer가 없다. exact binary32 `4.0`, shared `30.0`, `15.0`의 SHA-256은 `4f4b9b7d8b86633e2824e2f439819357b0cd010ab410ea1a691b12c5f94e91e0`, `409303c5035263c102682239f8d654e7e194daae6235aff347c036576a261d96`, `a5d4cd5b9cc4aba67ebe403c68466c378868947703d75704381d0c90e2615d1e`다.

원본 chest는 chest→unit nil과 inventory count zero를 순서대로 단락한다. subclass 방향 bit가 있으면 대응 diagonal을, 없으면 unit position에서 chest position을 뺀 벡터를 normalize하고 shape extent에 `4.0+15.0`을 더해 중심 후보를 만든 뒤 perpendicular `30.0` 오프셋 두 개를 만든다. unit과의 세 squared distance를 x87 비교로 재배열하고 chest 위치를 origin으로 후보 `2→1→0` 순서로 trace하며, 각 trace의 whole EAX가 zero일 때만 그 후보를 live unit X/Y로 대체한다. inventory count가 정확히 1이면 first item 하나를, 그 밖에는 successor를 먼저 얻는 순회로 각 item의 byte `+488 != 0xFF`와 class 저위 Monster bit 부재를 검사한다. 통과한 item은 live flags에 `0x40`을 OR하고 refresh한 뒤 후보를 `0→1→2`로 순환해 native drop dispatcher에 넘기며 반환은 버린다. helper는 shape kind 2에서 radius를 그대로 반환하고, kind 3에서 첫 extent가 ordered strict greater일 때만 그 절반을 선택하며 equal·less·unordered에서는 둘째 extent의 절반을, 나머지 kind에서는 exact positive zero를 반환한다. 구현 결속 전 누적 오라클은 **코드 400개·데이터 185개**이며 다음 주소 경계는 raw `004EE2F0`이다. 다음 단계는 chest와 helper의 x87·callback/fault 의미 계약 및 native 결속이다.

Chest의 직접 의존 함수 `00509F20..00509F59` 58바이트와 NOP `00509F5A..00509F5F` 6바이트의 SHA-256은 각각 `2ae9e2811d0221b06b047bdd7b96e75ddd23e324d5f99194ca232e4c5ef23087`, `ff35ffe14925642da6f3a258b35811e08101c03f8b5db346e5afcca448677564`이고 결합 64바이트는 `d5d7bf07a0cb9d8f46dd092aaf82168c8a5cce6175b7ad4297b790841a6e2633`다. decoded direct rel32 caller는 `0045CA89`, `0045D709`, `004E2845`, chest 내부 `004EDFB1`, `0054EDD2`, `0054F24D`, `0054F4D8`의 일곱 곳이며 direct jump와 저장된 little-endian absolute entrypoint는 없다. 원본은 Y를 X보다 먼저 적재하고 binary32 두 성분의 제곱합과 제곱근을 x87 53-bit 정밀도로 계산한 뒤 길이를 정확히 한 번 binary32로 반올림한다. 이어 live X를 길이로 나누어 binary32로 저장한 뒤에만 live Y를 읽고 같은 길이로 나누어 저장한다. 따라서 큰 두 성분의 길이가 binary32 infinity로 넘치면 finite 입력도 zero가 되고, subnormal·signed zero·zero-vector NaN도 일반적인 고수준 hypot 정규화와 다를 수 있다. 누적 오라클은 **코드 402개·데이터 185개**다. Chest 결속은 이 의존 함수를 같은 spill 경계의 순수 Go로 사용하며, 나머지 여섯 caller의 전역 포팅은 주장하지 않는다.

구현 결속은 `a4797efc6/d73be03da/2f249d82a/388937ee8/834694b8a/5250d5bdc/3d5658d6c`로 chest 오라클·순수 의미·native server·normalize 오라클/구현·Go 호출 경로·C ABI를 분리했다. raw chest와 private shape helper는 provenance-only이며 retained public CGo 경계는 `void nox_xxx_chest_4EDF00(nox_object_t*, nox_object_t*)`다. Go 1.26.5 macOS/ARM64 표적 10회·race/checkptr 각 3회·전체 server 3회와 macOS/AMD64 CGo 표적, Darwin 두 ISA의 C11/CGo frontend를 통과했다. Mach-O/ARM64 `server.test`에서 원본 chest 917/928바이트와 normalize 58/64바이트 pattern은 모두 0건이다. `make oracle-test`는 코드 402개·데이터 185개와 원본 트리 전후 동일성을 재확인했다. 저빈도 정책에 따라 전체 9-tuple 행렬은 실행하지 않았으며, Chest는 FoodDrop 기준점 뒤 첫 macOS-only 단위 `1/19`다. 다음 순차 감사 대상은 AudEventDrop `004EE2F0`이다.

AudEventDrop 본체 `004EE2F0..004EE367` 120바이트와 NOP `004EE368..004EE36F` 8바이트의 SHA-256은 각각 `4c1469a76c90beca37561beb42c619fb57cce90df926d9536a412073822aecc6`, `9e8376b4aa602de084708bf231f7ab5bd700e3d623bcf47a3851ce49cbe46f08`이고 결합 128바이트는 `38f3fe71ff12996c2c7671e114ce96022f2cf35019c40b4b56f2dfd21f3452d1`다. direct rel32 call/jump는 없으며 little-endian absolute entrypoint는 정렬된 `.data` file offset `0x001C9DD8`, VA `005C9DD8`에 정확히 한 번 있다. `005C9DD4`의 12바이트 등록 레코드는 이름 `005C9E68`, callback `004EE2F0`, parser `00536AC0`을 결속하며 SHA-256은 `d375e52ca90831a48c26fd928255953522a0e90de7cdc2ee826e99883b4b9088`다. 정렬을 포함한 `AudEventDrop\0` 16바이트 SHA-256은 `cd8665f47376f984b261899c45189fac8ef015a60e5b3f3eed823681c71d5014`다.

원본 callback은 owner→item→point 포인터를 순서대로 검사해 하나라도 nil이면 canonical zero를 반환하고, 모두 non-nil일 때 DefaultDrop을 호출한다. whole signed EAX가 zero이면 그대로 끝나지만 모든 nonzero 값은 마지막까지 cache해 exact 반환한다. 성공 뒤 table 첫 type word를 item보다 먼저 읽어 `0xffff` sentinel이면 TypeInd를 읽지 않는다. nonempty일 때만 item TypeInd를 zero-extended 16비트로 한 번 cache하고 4바이트 `{type,sound}` row를 순서대로 훑는다. 첫 type match는 sound가 zero여도 zero-extended sound로 owner audio를 한 번 호출하고, sentinel miss는 audio 없이 cached DefaultDrop 결과를 반환한다. 다음 함수는 AnkhTradableDrop `004EE370`이다.

결속 parser `00536AC0..00536B3D` 126바이트와 NOP `00536B3E..00536B3F` 2바이트의 SHA-256은 각각 `d8c769589af49a19efd67dc4ccf33f5f579e84db6e03903e604ce825ae4c92b1`, `182003d5c37dc5253d84cc5156ca9f93aab75e72e395d157748de67cc20f4f76`이고 결합 128바이트는 `adbb2d857b4725719909f5aeef219f44f73bc9625ead5fc7be7baae921107771`다. direct rel32 call/jump는 없고 stored absolute pointer는 위 등록 레코드의 `005C9DDC` 한 곳뿐이다. parser는 최초 호출에서 51개 row를 `{0xffff,0}`으로 초기화하고 init flag를 마지막에 쓴다. 이후 first sentinel을 찾되 live row 50개면 token이나 type 인수를 읽지 않고 끝낸다. 빈자리가 있으면 exact `" \t\n\r"` 5바이트 delimiter로 다음 token을 얻고 nonempty token의 sound ID가 nonzero일 때만 type 인수를 읽어 sound를 먼저 쓴 뒤 type을 써 row를 publish한다. 따라서 duplicate TypeInd는 append되고 callback의 first-match가 최초 값을 선택한다. delimiter SHA-256은 `fdf62e5430e9ae456a2c76fd887f2a82445e6d1240036b2e9347c77fdc9a0809`다. runtime table `00977CA0`과 init flag `00833E98`은 PE 파일-backed data가 아닌 zero-filled 주소이므로 코드 동작으로 고정한다. 누적 오라클은 **코드 406개·데이터 188개**이며 다음 단계는 callback·parser의 event/fault 계약과 native 결속이다.

구현 결속은 `c5d3a862b/86b3c5604/9d2ae13d1/be71a98ab/4d5cb329f`로 오라클·순수 의미·native server·legacy 등록 경로·C ABI를 분리했다. callback의 exact nonzero `int32`, sentinel-before-TypeInd, TypeInd single load, zero sound 포함 first match와 parser의 51행 초기화·50행 cap·sound-before-type publish·duplicate first-wins를 event/fault 계약으로 고정했다. 기존 unordered map은 ordered native table로 교체했고 stale root bool/value-copy wrapper는 제거했다. exact retained ABI는 `int nox_objectDropAudEvent_4EE2F0(nox_object_t*, nox_object_t*, float2*)`다.

Go 1.26.5 macOS/ARM64 표적 10회·race/checkptr 각 3회·전체 server 3회, macOS/AMD64 CGo server 표적과 Darwin 두 ISA의 C11/CGo frontend를 통과했다. ARM64/AMD64 Mach-O `server.test`에서 원본 callback 120/128바이트와 parser 126/128바이트 pattern은 모두 0건이다. `make oracle-test`는 코드 406개·데이터 188개와 원본 트리 전후 동일성을 재확인했다. 저빈도 정책에 따라 전체 9-tuple 행렬은 실행하지 않았으며, AudEventDrop은 FoodDrop 기준점 뒤 두 번째 macOS-only 단위 `2/19`다. 남은 macOS-only 단위는 17개이고 다음 순차 감사 대상은 AnkhTradableDrop `004EE370`이다.

AnkhTradableDrop `004EE370..004EE387` 24바이트와 NOP `004EE388..004EE38F` 8바이트의 SHA-256은 각각 `5325d2c5848f54654df86e785d020823d97fc953410134ede5d1da9c72087a7d`, `9e8376b4aa602de084708bf231f7ab5bd700e3d623bcf47a3851ce49cbe46f08`이고 결합 32바이트는 `d2e11748ea2b1be9d2757b8afbca80cb9dd1bb131faad5fc5a3f02ba2783fa1f`다. 원본은 point→item→owner 순서로 세 stack argument를 읽고 같은 세 값을 owner/item/point 순서로 DefaultDrop `004ED290`에 넘긴 뒤 whole EAX를 그대로 반환한다. direct incoming rel32 call/jump는 없고 outbound call instruction `004EE37F`의 SHA-256은 `a69fe4d0dc34b415816689b5d086dad1507495dca9fbf7a7c7751dfa8634c16f`다. little-endian absolute entrypoint는 registration callback cell인 file offset `0x001C9DE4` 한 곳뿐이다.

`005C9DE0`의 12바이트 등록 레코드는 이름 `005C9E78`, callback `004EE370`, null parser를 결속하며 SHA-256은 `03cf5d6535fe773e4149f04ac1c4cd8a7b6e09cf943c666a1f2492dbad7e5833`다. `005C9E78`의 `AnkhTradableDrop\0`과 정렬을 포함한 20바이트 SHA-256은 `36f3a21a48cecff14889483807f7d03da9f6a4d398fe7942d22d811c0aada3df`다. 다음 함수는 `004EE390`이며, 누적 오라클은 **코드 408개·데이터 190개**다. 다음 단계는 thunk의 인수 전달·exact 반환 계약과 현재 `int` 기반 C 경계를 native pointer로 복원하는 것이다.

구현 결속은 `2666ae96b/217181613/1714d332e/b01d6617c/4c52b1abf`로 오라클·순수 의미·native server·legacy 등록 경로·C ABI를 분리했다. point→item→owner 선행 load와 cache, nil 무가드 전달, DefaultDrop whole `int32` 반환을 event/fault 계약으로 고정하고 등록 경로를 Go callback으로 전환했다. exact retained ABI는 `int nox_xxx_dropAnkhTradable_4EE370(nox_object_t*, nox_object_t*, float2*)`다. Go 1.26.5 macOS/ARM64 표적 10회·race/checkptr 각 3회·전체 server 3회와 당시 정책의 macOS/AMD64 CGo server 및 Darwin 두 ISA C11/CGo frontend를 통과했다. ARM64/AMD64 Mach-O `server.test` SHA-256은 `ec10bc52b94439b5db0eeb27a5d50acadad2a5ae5abd469f02e3c3bc09266cd1`, `fb9ebe69f3f2347ce0e00047c1d862e223c36fe8cec3001c7784739725b6fbd6`이며 원본 24/32바이트 pattern은 모두 0건이다. `make oracle-test`는 코드 408개·데이터 190개, 원본 트리 전후 동일성과 NXZ strict 50쌍을 재확인했다. 전체 9-tuple 행렬은 실행하지 않았으며 AnkhTradableDrop은 FoodDrop 기준점 뒤 세 번째 단위 `3/19`, 다음 전체 행렬 전 남은 단위는 16개다. 다음 `sub_4EE390`부터 상시 게이트를 macOS/ARM64 하나로 좁히고, macOS/AMD64를 포함한 전체 9-tuple은 20개 단위마다 한 번만 실행한다.

과거 소스에서 `nox_xxx_unused_4EE390`으로 불린 본체 `004EE390..004EE3DD` 78바이트와 NOP `004EE3DE..004EE3DF` 2바이트의 SHA-256은 각각 `245090b40e79d3d27b94db14ad229f1b0963f5852ae3b2598ee3437e5f8c1c01`, `182003d5c37dc5253d84cc5156ca9f93aab75e72e395d157748de67cc20f4f76`이고 결합 80바이트는 `f9d8d16cdbb012d77e68f5c8307e83b8e6906301c5b4459f3078b94aa5e87e20`이다. decoded direct incoming rel32 call/jump와 whole-file little-endian absolute entrypoint는 모두 0개다. BSS head `00753398`의 세 operand cell은 `004EE3C0/004EE3C9/004EE3D9`이지만 모두 아래의 확정 fault 뒤에 있다. 다음 함수는 unused health-link removal `004EE3E0`이다.

원본은 nil 객체를 그대로 반환한다. non-nil 객체에서는 `HealthData +556`을 읽고 non-nil이면 그 레코드의 `+12`를 0으로 쓴 뒤 객체의 HealthData를 다시 읽어 재로드된 레코드의 `+8`을 0으로 쓰고 그 포인터를 EAX로 반환한다. 반대로 HealthData가 nil이면 전역 head를 읽기 전에 absolute `0x0000000C`에 0을 쓰므로 정상 Windows 메모리 모델에서 즉시 fault한다. 그 뒤의 head load/store와 `head.ObjectPrev +448` 갱신은 도달 불가능하지만 원본 명령열로 보존한다. 과거 OpenNox C 본체는 `b99fbcf5d`에서 unused로 삭제됐으며, 이번 단계에서도 존재하지 않는 caller·registration·C ABI를 새로 만들지 않는다. 누적 오라클은 **코드 410개·데이터 190개**다.

구현 결속은 `229102b72/315f789ce/caca05851`로 오라클·순수 의미·native server를 분리했다. generic hook 계약은 nil 객체 무접근, 두 link store 사이의 live HealthData reload, absolute-null fault와 시험 전용 continuation을 통한 사후 dead instruction stream 및 모든 fault prefix를 고정한다. native adapter는 `Object.HealthData`와 고정폭 `HealthData.field8/field12`에 결속하고 nil HealthData의 `0x0000000C` fault를 panic으로 보존한다. 호출 근거가 없으므로 production C/Go 호출자나 public CGo ABI는 만들지 않았다. Go 1.26.5 macOS/ARM64 표적 10회·race/checkptr 각 3회·전체 server 3회와 ARM64 Mach-O 직접 실행 10회를 통과했다. 제품 SHA-256은 `331dfb1c547bf90337384626e2b9e58f5c6ecebd9327b56170d83b24b84cf6ec`이고 원본 78/80바이트 pattern은 모두 0개다. `make oracle-test`는 코드 410개·데이터 190개, 원본 트리 전후 동일성과 NXZ strict 50쌍을 재확인했다. 전체 9-tuple 행렬은 실행하지 않았으며 이 함수는 FoodDrop 기준점 뒤 네 번째 ARM64-only 단위 `4/19`, 다음 전체 행렬 전 남은 단위는 15개다. 다음 순차 감사 대상은 unused health-link removal `004EE3E0`이다.

과거 소스에서 `nox_xxx_unused_4EE3E0`으로 불린 본체 `004EE3E0..004EE427` 72바이트와 NOP `004EE428..004EE42F` 8바이트의 SHA-256은 각각 `051f289e3ee2ef5d1557dab8efc02d2adccfc5cc3266fb2a6a533998da2caa4a`, `9e8376b4aa602de084708bf231f7ab5bd700e3d623bcf47a3851ce49cbe46f08`이고 결합 80바이트는 `f74f0042ba324227cb3461a6deaea8926d802968f512939f7032680da93311f5`다. decoded direct incoming rel32 call/jump와 whole-file little-endian absolute entrypoint는 모두 0개다. BSS head `00753398` store operand는 `004EE423` 한 곳이고 다음 함수는 head getter `004EE430`이다.

원본은 nil 객체와 nil 초기 HealthData를 무효과로 반환한다. 초기 HealthData의 `+8` next가 non-nil이면 next 객체의 HealthData를 먼저 읽고, 그 뒤 초기 HealthData의 `+12` previous를 읽어 next HealthData의 `+12`에 쓴다. 이어 객체의 HealthData를 live reload하고 재로드된 레코드의 `+12` previous를 읽는다. previous가 non-nil이면 그 객체의 HealthData를 먼저 읽은 뒤 재로드된 레코드의 `+8` next를 읽어 previous HealthData의 `+8`에 쓴다. previous가 nil이면 재로드된 `+8`을 전역 head에 쓴다. 제거한 레코드 자체의 두 link는 지우지 않으며, 중간의 null HealthData는 별도 방어 없이 원본 위치에서 fault한다. 과거 C 본체는 `b99fbcf5d`에서 unused로 삭제됐고 정의·선언 밖의 역사적 호출도 없다. 누적 오라클은 **코드 412개·데이터 190개**다.

구현 결속은 `a791d40e7/55cba259f/fbf54a113`으로 오라클·순수 의미·native server를 분리했다. generic 계약은 고립 head/head/middle/tail, 두 번 관찰되는 객체 HealthData의 변이, successor/predecessor HealthData 뒤 live link load, 제거 레코드 link 비변조와 모든 fault prefix를 고정한다. native adapter는 `*Object` next/previous와 head를 `Server`의 `map[*HealthData]*Object` sidecar에 두며 고정폭 ABI32 두 필드에는 포인터를 쓰지 않는다. 앞선 reset도 이 sidecar를 함께 clear한다. 호출 근거가 없으므로 production C/Go caller나 public CGo ABI는 만들지 않았다. Go 1.26.5 macOS/ARM64 표적 10회·race/checkptr 각 3회·전체 server 3회와 ARM64 Mach-O 직접 실행 10회를 통과했다. 제품 SHA-256은 `b20319e8a87be9d724bfa9baf82e219d9b99587bd410855d53813026c9a21376`이고 원본 72/80바이트 pattern은 모두 0개다. `make oracle-test`는 코드 412개·데이터 190개, 원본 트리 전후 동일성과 NXZ strict 50쌍을 재확인했다. 전체 9-tuple 행렬은 실행하지 않았으며 이 함수는 FoodDrop 기준점 뒤 다섯 번째 ARM64-only 단위 `5/19`, 다음 전체 행렬 전 남은 단위는 14개다. 다음 순차 감사 대상은 head getter `004EE430`이다.

과거 소스의 `sub_4EE430` 본체 `004EE430..004EE435` 6바이트와 NOP `004EE436..004EE43F` 10바이트의 SHA-256은 각각 `0ee05f33f0059d24b8957734e893a04241c9d4ed99e7515285ae972f9ce01230`, `bde559b24d3a5302d82a4e56eb6f4b12d39057d100fd0ca81b337f5c1aa80cba`이고 결합 16바이트는 `0892f29333ac88c4046cef6ca8e63bf7ab0b82aa9825b8edabdb72ea4f76e81b`다. decoded direct incoming rel32 call/jump와 whole-file little-endian absolute entrypoint는 모두 0개다. BSS head `00753398` load operand는 `004EE431` 한 곳이며 원본은 이 dword를 정확히 한 번 읽어 같은 객체 포인터를 반환한다. 과거 C getter는 `d15e0aece`에서 unused로 삭제됐고 정의·선언 밖의 역사적 호출도 없다. 다음 함수는 next-link getter `004EE440`이고 누적 오라클은 **코드 414개·데이터 190개**다.

구현 결속은 `d01649ca4/a1e75725a/916e57273`으로 오라클·순수 의미·native server를 분리했다. generic 계약은 단일 head load의 횟수, nil과 pointer identity, load 직후 live head 변이 및 load fault를 고정한다. native adapter는 `healthLinksState4EE390.head *Object`를 그대로 반환하며 호출 근거가 없어 public CGo ABI를 만들지 않았다. Go 1.26.5 macOS/ARM64 표적 10회·race/checkptr 각 3회·전체 server 3회와 ARM64 Mach-O 직접 실행 10회를 통과했다. 제품 SHA-256은 `4cae051b7319c6efb57a1ebb356ed466cca9b75c9beede1a4efedefa39251740`이고 원본 6/16바이트 pattern은 모두 0개다. `make oracle-test`는 코드 414개·데이터 190개, 원본 트리 전후 동일성과 NXZ strict 50쌍을 재확인했다. 전체 9-tuple 행렬은 실행하지 않았으며 이 함수는 FoodDrop 기준점 뒤 여섯 번째 ARM64-only 단위 `6/19`, 다음 전체 행렬 전 남은 단위는 13개다. 다음 순차 감사 대상은 next-link getter `004EE440`이다.

과거 소스의 `sub_4EE440` 본체 `004EE440..004EE454` 21바이트와 NOP `004EE455..004EE45F` 11바이트의 SHA-256은 각각 `4716b3f4ce71c54d3031f90f4dddc3791a9b991e3be91e2fd5eaaeb2350f0321`, `19f3c2045194c5d2e45451e3dfe6a203b5e240aec5a2400a92cdb425c3331137`이고 결합 32바이트는 `c207d227cf01783e3c121bb103fe0275f06d6de16ab7971414219af6fddc8e71`이다. decoded direct incoming rel32 call/jump와 whole-file little-endian absolute entrypoint는 모두 0개다. nil 객체는 객체 메모리를 읽지 않고 xor-zero nil을 반환한다. non-nil 객체는 `HealthData +556`을 한 번 읽고 그 레코드의 `+8` next를 반환하며 HealthData nil guard는 없어 정확한 두 번째 접근에서 fault한다. 과거 C getter는 `391c59fd0`에서 unused로 삭제됐고 정의·선언 밖의 역사적 호출도 없다. 다음 함수는 unit HP adjustment `004EE460`이고 누적 오라클은 **코드 416개·데이터 190개**다.

구현 결속은 `439b40914/e0600b740/629d50117`로 오라클·순수 의미·native server를 분리했다. generic 계약은 nil 객체 무접근, HealthData와 next의 정확한 단일 load, 각 load 직후 live 값 변이에도 이미 읽은 값을 쓰는 동작, nil HealthData의 두 번째 load fault와 모든 fault prefix를 고정한다. native adapter는 `Object.HealthData`와 pointer-width sidecar의 `next map[*HealthData]*Object`에 결속하고 ABI32 `field8` sentinel을 보존한다. 호출 근거가 없어 public CGo ABI는 만들지 않았다. Go 1.26.5 macOS/ARM64 표적 10회·race/checkptr 각 3회·전체 server 3회와 ARM64 Mach-O 직접 실행 10회를 통과했다. 제품 SHA-256은 `d8a25b493c742ceea88ad35d93acfc358f3a087a9189f37868a490b42f9acf17`이고 원본 21/32바이트 pattern은 모두 0개다. `make oracle-test`는 코드 416개·데이터 190개, 원본 트리 전후 동일성과 NXZ strict 50쌍을 재확인했다. 전체 9-tuple 행렬은 실행하지 않았으며 이 함수는 FoodDrop 기준점 뒤 일곱 번째 ARM64-only 단위 `7/19`, 다음 전체 행렬 전 남은 단위는 12개다. 다음 순차 감사 대상은 unit HP adjustment `004EE460`이다.

원본 `nox_xxx_unitAdjustHP_4EE460` 본체 `004EE460..004EE4BF` 96바이트와 결속된 `nox_xxx_mobInformOwnerHP_4EE4C0` 본체 `004EE4C0..004EE4F6` 55바이트, NOP `004EE4F7..004EE4FF` 9바이트의 SHA-256은 각각 `c9b5645ed335cb744576b6539da35a3b8715735eb8f25ac9a085ff9f2b7bf7f1`, `c31f356f1dcfd8355415efc424dc61721c9818ffd65f187e8d1ff7c220a02dd1`, `f56642978961c41b24911838d549a9957c25a0dee0914c9230b5f17a3567418b`이다. helper+padding 64바이트와 전체 160바이트 클러스터의 SHA-256은 `90d8bae4e6398c7e034426859e5ee026fbe241a92ae744f8e134d33a09a90bf1`, `4093d7cbcde15fe7ddabeecb0936db6d91f3b7587d4d4ba75a5389986f8991c8`이다. decoded direct incoming rel32 call은 메인 13곳 `004E0290/004E0821/004E082F/004E10DE/004F38FB/004F9F41/0050A726/00513E10/0052DE11/0052F269/0052F441/0053EE53/0053F01C`, helper 3곳 `004EE4B6/004EE6DE/004EE726`이며 direct jump와 whole-file little-endian absolute entrypoint는 둘 다 0개다.

메인은 game flag `0x04000000`을 객체보다 먼저 검사해 nonzero면 끝난다. zero이면 nil 객체 guard 없이 HealthData를 읽고, nil HealthData 또는 unsigned `Cur >= Max`면 delta를 읽지 않고 끝난다. `Cur < Max`에서는 `int32(delta)+uint16(Cur)`를 32비트 wrapping하고 그 결과를 signed 비교해 `>= uint16(Max)`일 때만 Max로 clamp하며, 음수·overflow 결과는 저위 16비트를 HP setter에 전달한다. setter 뒤 live 객체 class 저위 byte의 Monster bit `0x02`가 켜졌을 때만 helper를 부른다. helper는 nil 객체와 nil owner를 단락하고 owner의 live Player bit `0x04`를 검사한 뒤, owner UpdateData→Player→PlayerInd byte를 중간 nil guard 없이 읽어 객체와 함께 current-HP network reporter에 넘긴다. 현재 Go와 C 양쪽에 호출자가 남아 있어 두 함수는 한 포팅 클러스터로 처리한다. 다음 함수는 solo monster kill reward `004EE500`이고 누적 오라클은 **코드 419개·데이터 190개**다.

직접 의존하는 `nox_xxx_netReportUnitCurrentHP_4D8620` 본체 `004D8620..004D8667` 72바이트와 NOP `004D8668..004D866F` 8바이트의 SHA-256은 각각 `7f0b055daab10a99d1ef618fd23ae528bc038e8e9faffa1a45df8f65bfdd3df4`, `9e8376b4aa602de084708bf231f7ab5bd700e3d623bcf47a3851ce49cbe46f08`이고 결합 80바이트는 `31eda7416f96bb38e3bfcdf385aa8cdcda6e6544431a72fbec311c3a3561fa18`이다. decoded direct incoming rel32 call은 `004EE4EE/00527EE2` 두 곳이고 direct jump와 whole-file little-endian absolute entrypoint는 0개다.

reporter는 object 인수를 먼저 읽고 초기 HealthData가 nil이면 zero를 반환한다. non-nil이면 opcode `65`를 기록하고 객체의 unit net code를 16비트로 cache한 뒤 객체 HealthData를 live reload한다. 재로드된 레코드는 nil guard 없이 Cur를 읽으므로 net-code callback이 HealthData를 nil로 바꾸면 그 위치에서 fault한다. Cur를 읽은 뒤에야 recipient 인수를 읽고, unsigned 우측 1비트 이동한 Cur의 저위 byte를 packet byte 3에 써 `{65, netCodeLo, netCodeHi, byte(Cur>>1)}`를 길이 4·related object nil·disconnect removal 1·reliable 1로 전송해 whole `int32` 결과를 반환한다. 이 의존성도 native-width 객체 경로로 옮기며 누적 오라클은 **코드 421개·데이터 190개**다.

구현 결속은 `70003a5e8/93443873b/89aed0bee/1590b29df/0cd145a22/0c0f984bc/dc3560339`로 main/helper 오라클·의미, reporter 오라클·의미, native server, legacy 전환과 C ABI를 분리했다. main의 flag/객체/HealthData/Cur/Max/delta/setter/live-class 순서와 32비트 wrapping, helper의 owner/UpdateData/Player/PlayerInd 순서, reporter의 두 HealthData load와 exact packet을 모든 fault prefix까지 고정했다. 세 raw C 본체는 provenance-only이고 active 경계는 `void nox_xxx_unitAdjustHP_4EE460(nox_object_t*, int)`, `void nox_xxx_mobInformOwnerHP_4EE4C0(nox_object_t*)`, `int nox_xxx_netReportUnitCurrentHP_4D8620(int, nox_object_t*)`다. setter `004E4560`과 정수 유래 객체를 넘기는 상위 C caller는 별도 복원 범위로 남긴다.

Go 1.26.5 macOS/ARM64 표적 10회·race/checkptr 각 3회·전체 server 3회, ARM64 Mach-O 직접 실행 10회, 독립 C11과 생성 CGo header/export/wrapper/main 객체 컴파일을 통과했다. `server.test` SHA-256은 `a05a585d4eeb73ada1bccb0169530b8692e2a91ca94bde0460159ab4e550acfb`이고 원본 여섯 body/cluster pattern은 모두 0개다. 전체 `make oracle-test`는 원본 트리 전후 동일성, 코드 421개·데이터 190개와 NXZ strict 50쌍을 통과했다. 이식성 집계는 `go_layout 1611/225`, `go_pointer_conversion 433/183`, `go_unsafe 3903/488`, `c_static_assert 1245/135`, `x86_isa 111/58`, `c_pointer_integer_cast 624/48`, `unsafe_literal_offset 202/35`, `cgo_import 226/226`이다. 전체 macOS `legacy`는 기존 Win32 고정 layout assertion에서 중단되며 전체 9-tuple 행렬은 저빈도 정책에 따라 실행하지 않았다. 이 클러스터는 FoodDrop 기준점 뒤 여덟 번째 ARM64-only 단위 `8/19`, 다음 전체 행렬 전 남은 단위는 11개이고 다음 순차 감사 대상은 `004EE500`이다.

원본 `nox_xxx_soloMonsterKillReward_4EE500_obj_health` 본체 `004EE500..004EE5D5` 214바이트와 NOP `004EE5D6..004EE5DF` 10바이트의 SHA-256은 각각 `ffd9019e735c23fe67d9032e38987ba5de60eec81b1b0ac07b709491c36a1fa3`, `bde559b24d3a5302d82a4e56eb6f4b12d39057d100fd0ca81b337f5c1aa80cba`이고 결합 224바이트는 `22b3305f43c588f16de772eb15580fe0c0448109c51fbb6ae93709b28b4df7b8`이다. decoded direct incoming rel32 call은 보통 사망 경로 `004EE696`과 zombie burn 삭제 경로 `00544D36` 두 곳뿐이며 instruction SHA-256은 각각 `2a24563b26ffb49b1b8a5cb5e4fed436d7d0a372902e66b72df1b64ff697a786`, `e665f11d253aea1ca3100a0fd8415c88c51ccedee86267fbeab86a90287650fc`다. direct jump와 whole-file little-endian absolute entrypoint는 모두 0개다. `005B9494`의 NUL 종료 원본 소스 경로 38바이트와 `005B94BC`의 `gainpoints\0` 11바이트도 각각 SHA-256 `f994b7f03a8fb9e512d87ebaf62eeca0dc1cb3038e9931e557761585a0113333`, `9deb5f510dec1953eaa9914dbfa117e777c5b264933b353a8538a073f266a8aa`로 봉인했고, 공유 positive-zero `0058307C`에는 XP 비교 용도를 추가했다.

원본은 nil killed-object와 cooperative flag `0x800` zero를 순서대로 단락한 뒤 `Obj130 +520`을 한 번 읽고 nil이면 끝난다. cached attribution object로 owner-chain Player를 찾은 다음 반환값의 Player class bit `0x04`를 nil guard 없이 검사한다. attribution이 Player 자체가 아니면 owner chain을 걷되 첫 Monster class bit `0x02` 객체만 monitored 판정을 한 번 수행하고, 그 callback 뒤에도 해당 객체의 live owner를 한 번 읽는다. 첫 Monster 뒤의 다음 반복에서는 더 이상 class나 owner를 읽지 않으며 monitored 결과가 zero일 때만 보상을 거부한다. Monster 없이 Player에 도달하면 허용하고, 순환 chain에는 별도 방어가 없다. 허용 경로는 원래 killed-object 인수를 다시 사용해 binary32 Experience를 읽고 `004EF270`에 넘긴다. 반환된 x87 값이 zero 이하이거나 unordered이면 pop 후 종료하고, strictly positive이면 `00566DCC`와 같은 signed int64 0쪽 절단의 하위 32비트를 보존해 localization lookup 뒤 line-message의 세 번째 인자로 넘긴다. 누적 오라클은 **코드 425개·데이터 192개**이고 다음 순차 함수는 unit damage clear `004EE5E0`이다.

구현 결속은 `796a92833/2b2f70f8a/efdf644cd/2e8bbd9d0/8023170e4`로 오라클·순수 의미·native server·legacy 전환·C ABI를 분리했다. generic 계약은 nil/flag/attribution/Player/Monster 순서와 모든 fault prefix, 첫 Monster 단일 callback, callback 뒤 live owner, strictly-positive x87 gate와 signed-int64 truncation low dword를 고정한다. native adapter는 `Object.Obj130`, `ObjOwner`, `ObjClass`, `Experience`, 기존 native Player lookup/monitored, `StringManager.GetStringInFile`에 결속했다. raw C 본체는 provenance-only이고 active ABI는 `void nox_xxx_soloMonsterKillReward_4EE500_obj_health(nox_object_t*)`다. 단, XP service `004EF270`은 아직 `(int)(uintptr_t)` shim을 거치므로 별도 ABI32 의존으로 남긴다.

Go 1.26.5 macOS/ARM64 표적 10회·race/checkptr 각 3회·전체 server 3회, ARM64 Mach-O 직접 실행 10회, 독립 C11 O0/O2·ASan/UBSan과 생성 CGo header/export/wrapper/main의 엄격한 ARM64 객체 컴파일을 통과했다. `server.test` SHA-256은 `81a159184fd34df71081a4f53fb58310f0c75d51649140137031be9565333be8`이고 원본 214/224바이트 pattern은 0개다. 전체 `make oracle-test`는 원본 트리 전후 동일성, 코드 425개·데이터 192개와 NXZ strict 50쌍을 통과했다. 전체 macOS `legacy`는 기존 Win32 고정 layout assertion에서 중단되며 9-tuple 행렬은 저빈도 정책에 따라 실행하지 않았다. 이 함수는 FoodDrop 기준점 뒤 아홉 번째 ARM64-only 단위 `9/19`, 다음 전체 행렬 전 남은 단위는 10개다.

원본 `nox_xxx_unitDamageClear_4EE5E0` 본체 `004EE5E0..004EE6E7` 264바이트와 NOP `004EE6E8..004EE6EF` 8바이트의 SHA-256은 각각 `2aa2fdff219fdcd15ffdaaf41783e5b00e412ad76fa4a7fdf74baf4449f8fbdf`, `9e8376b4aa602de084708bf231f7ab5bd700e3d623bcf47a3851ce49cbe46f08`이고 결합 272바이트는 `c5601116242c18b5432e4a4eb1f11f0d8d12519853e62bb81d6ab7bde8bda36c`이다. raw `.text` rel32 scan과 명령 디코드는 direct call `0041A447/004E0933/004E1212/004E8658/004E975F/004F9EBA/004FF6B1/0052F49E/0052F590` 아홉 곳과 direct jump 0곳에 일치하며 whole-file little-endian absolute entrypoint도 0개다. `004E8658/004E975F`는 이미 봉인된 Player/Bomb collision 본체 안에 있고 나머지 일곱 instruction을 독립 범위로 추가했다. `004EE696`의 solo-reward call은 새 본체에 포함되므로 겹치던 독립 범위를 제거했다.

원본은 nil unit, 초기 HealthData nil, 초기 Max zero를 순서대로 단락한 뒤 GodMode byte를 읽는다. GodMode가 켜진 Player만 종료하고, 이어 live Player class를 다시 검사해 cached UpdateData→Player→Harpoon flag와 cached UpdateData의 harpoon slot을 읽어 조건부 break를 호출한다. 그 뒤 HealthData와 damage 인수를 새로 읽고 zero-extended Cur와 signed damage를 비교한다. signed `Cur > damage`이면 저위 16비트 차를 setter에 넘기고, 아니면 HP zero 뒤 live flags에서 기존 Dead를 검사한다. 첫 사망은 Dead store→buff 16 제거→zombie 검사→조건부 solo reward→live Monster class에 따른 die callback 또는 live delete override/default delete 순서이며, 마지막에는 다시 live Monster class를 읽어 owner HP를 보고한다. 다음 단계는 이 반복 load와 callback/fault prefix, signed damage 경계를 순수 계약으로 고정하는 것이며 누적 오라클은 **코드 433개·데이터 192개**다.

구현 결속은 `30fa23441/e0c29eb31/1119afaf7/936fb790b/086a8051b`로 오라클·순수 의미·native server·legacy 전환·C ABI를 분리했다. generic 계약은 nil/HealthData/Max/GodMode 단락, GodMode 활성 시 class 이중 load, cached UpdateData와 unguarded Player, harpoon callback 뒤 live HealthData, damage 인수의 지연 load, signed 비교와 저위 16비트 래핑, setter 뒤 live flags, Dead store 뒤 buff/zombie/reward/class/death/delete 및 최종 live class를 모든 fault prefix와 함께 고정한다. native adapter는 pointer-width `Object.HealthData/UpdateData/Death`, `PlayerUpdateData.Player/HarpoonTarg`, `Player.Info.playerClass`와 native Death callback에 결속했고 raw `004EE5E0` C 본체는 provenance-only다. active ABI는 `void nox_xxx_unitDamageClear_4EE5E0(nox_object_t*, int)`이며 아홉 원본 caller 중 Player/Bomb collision과 sudden-death를 포함한 Go 경로는 CGo 왕복 없이 이 구현을 호출하고 나머지 여섯 C 경로는 같은 typed export로 들어온다. 단, HP setter `004E4560`, buff-off `004FF5B0`, Monster die `0050A3D0`과 XP service `004EF270`은 각각 별도 ABI32 복원 범위이고, 정수 유래 객체를 만드는 상위 C producer도 전역 widening 범위로 남긴다.

Go 1.26.5 macOS/ARM64 표적 10회·race/checkptr 각 3회·전체 server 3회, ARM64 Mach-O 직접 실행 10회, 독립 C11 O0/O2·ASan/UBSan과 생성 CGo export/wrapper의 엄격한 ARM64 객체 컴파일을 통과했다. `server.test` SHA-256은 `cdb0cea49335e44e6fafc57ec1f644ea04ce24378078ec6b2a2a34c2fe5d91e6`이고 원본 264/272바이트 pattern은 모두 0개다. 전체 `make oracle-test`는 원본 트리 전후 동일성, 코드 433개·데이터 192개와 NXZ strict 50쌍을 통과했다. 이식성 집계는 `go_layout 1638/227`, `go_pointer_conversion 435/185`, `go_unsafe 3941/491`, `c_static_assert 1250/137`, `x86_isa 112/59`, `c_pointer_integer_cast 624/48`, `unsafe_literal_offset 202/35`, `cgo_import 228/228`이다. 전체 macOS `legacy`는 기존 Win32 고정 layout assertion에서 중단되며 정책에 따라 macOS/AMD64·Linux·Windows와 전체 9-tuple 행렬은 실행하지 않았다. 이 함수는 FoodDrop 기준점 뒤 열 번째 ARM64-only 단위 `10/19`, 다음 전체 행렬 전 남은 단위는 9개이고 다음 순차 감사 대상은 unit maximum-HP restoration `004EE6F0`이다.

원본 `nox_xxx_unitHPsetOnMax_4EE6F0`은 `004EE6F0..004EE72F`의 정확히 64바이트이며 다음 `004EE730` 함수가 즉시 시작해 padding은 없다. SHA-256은 `21d881b1077a3bdc436f2b3b1fde8e56322917a2925ab2a1bb9733068ac88a07`이다. raw `.text` rel32 scan과 명령 디코드는 direct call `00441ED8/004EAA4E/004EF801/004EFFA1/0052BF2E/00534AF6` 여섯 곳과 direct jump 0곳에 일치하며 whole-file little-endian absolute entrypoint도 0개다. GameBall pickup 안의 `004EAA4E`는 이미 봉인된 `004EA800` 본체에 포함되고 나머지 다섯 instruction을 독립 범위로 추가했다.

원본은 nil unit과 초기 HealthData nil을 순서대로 단락하고 초기 레코드의 Max 저위 16비트를 HP setter에 전달한다. setter 뒤 unit HealthData를 live reload해 nil guard 없이 그 새 레코드의 Cur를 읽고 같은 레코드의 Field2에 쓴다. 마지막으로 live unit class 저위 byte를 읽어 Monster bit가 있을 때만 owner HP reporter를 호출한다. setter가 HealthData를 nil이나 다른 레코드로 바꾸는 경우의 fault·copy, setter가 class를 바꾸는 경우와 reporter 호출 순서까지 다음 의미 계약에서 고정해야 한다. 누적 오라클은 **코드 439개·데이터 192개**이고 다음 순차 함수는 player health initialization `004EE730`이다.

구현 결속은 `f9ab403b6/c0334df2d/163b7c515/31e305503/5f8f6cb7c/f06911f46`로 오라클·순수 의미·native server·legacy 전환·C ABI·Go 소유 caller를 분리했다. generic 계약은 nil 단락, 초기 HealthData와 Max cache, setter 뒤 live HealthData 재로드, Cur 단일 cache와 같은 live record Field2 store, 최종 live class와 Monster owner report를 모든 event/fault prefix로 고정한다. native adapter는 pointer-width `Object.HealthData`, `Object.ObjClass`, `HealthData.Cur/Field2/Max`에 결속했다. 원본 여섯 caller 중 health cheat와 GameBall은 CGo 왕복 없는 Go 경로로, 나머지 네 생산 C caller는 typed export로 들어온다. raw C 본체는 provenance-only이고 active ABI는 `void nox_xxx_unitHPsetOnMax_4EE6F0(nox_object_t*)`다. 네 C caller의 상위 객체 생산자는 아직 ABI32 정수이므로 전역 widening 범위에 남기고 HP setter `004E4560`도 별도 복원 범위로 유지한다.

clean functional revision `f06911f4640df679f141b4231dacb2231d335239`에서 Go 1.26.5 macOS/ARM64 표적 10회·race/checkptr 각 3회·전체 server 3회와 생성 Mach-O 직접 실행 10회를 통과했다. `server.test` SHA-256은 `aac7c66eb71f95b431e8d79c8b29b2406e8390cb91b636c0f6aec060edc7b533`이고 원본 64바이트 pattern은 0개다. 독립 C11 O0/O2·ASan/UBSan과 생성 CGo export/wrapper의 엄격한 ARM64 객체 컴파일도 통과했고, 전처리된 세 생산 C 번역 단위에는 typed 선언과 활성 호출만 남는다. 전체 `make oracle-test`는 원본 트리 전후 동일성, 코드 439개·데이터 192개와 NXZ strict 50쌍을 통과했다. 이식성 집계는 `go_layout 1647/228`, `go_pointer_conversion 436/186`, `go_unsafe 3950/492`, `c_static_assert 1250/137`, `x86_isa 112/59`, `c_pointer_integer_cast 625/48`, `unsafe_literal_offset 202/35`, `cgo_import 229/229`이다. 전체 macOS `legacy`는 기존 Win32 고정 layout assertion에서 중단되며 정책에 따라 macOS/AMD64·Linux·Windows와 전체 9-tuple 행렬은 실행하지 않았다. 이 함수는 FoodDrop 기준점 뒤 열한 번째 ARM64-only 단위 `11/19`, 다음 전체 행렬 전 남은 단위는 8개이고 다음 순차 감사 대상은 player health initialization `004EE730`이다.

원본 `nox_xxx_playerHP_4EE730`은 `004EE730..004EE77A`의 정확히 75바이트이고 `004EE77B..004EE77F`는 5바이트 NOP이며 다음 current-HP getter가 `004EE780`에서 시작한다. body·padding·결합 80바이트 SHA-256은 각각 `ff2b4b4f14b69d9c996cbbbc6e404e8c8078fad1c60b412d34de8df353283dbe`, `18e800921eac4b6ea289ffc28abb7e2d58e7521d3568dcacd9e3aa55096f35de`, `c34681e7157a9e1ed58b168853b201b6f4ed5ceb43140dbdf92f1695b927a83b`다. raw `.text` rel32 scan과 명령 디코드는 player-file loader의 sole direct call `0041A467` 한 곳, direct jump 0곳에 일치하고 whole-file little-endian absolute entrypoint도 0개다. call instruction SHA-256은 `00e4f7599d5b82be7283997df934ce556f6c706d3a2b0107ed93eb67e809b576`이다.

원본은 nil unit과 Player class 저위-byte bit를 먼저 단락한 뒤 초기 HealthData를 읽고 곧바로 UpdateData를 cache하며, 그 뒤에야 초기 HealthData nil을 검사한다. gate가 통과하면 cached UpdateData는 다시 읽지 않고, unit HealthData를 매 sample마다 live reload해 Cur를 읽고 `UpdateData +12..+75`의 32개 `uint16`에 차례대로 쓴다. 마지막에는 HealthData를 33번째로 별도 reload해 Cur를 `+76`에 쓴다. live HealthData와 cached UpdateData에는 nil guard가 없고 각 Cur는 store 전에 cache된다. 103개 관찰 이벤트의 모든 fault prefix, update pointer 교체, 매 sample HealthData 교체와 마지막 별도 reload를 순수 계약으로 고정했다. 누적 오라클은 **코드 442개·데이터 192개**이고 다음 순차 함수는 current-HP getter `004EE780`이다.

구현 결속은 `636700cd7/73e4c6bcc/2a5e3e22e/fb59d6d43/6ea3d7459`로 오라클·순수 의미·native server·legacy 전환·C ABI를 분리했다. `PlayerUpdateData +12..+75`를 `[32]uint16 HealthSamples`, `+76`을 `uint16 HealthSampleCur`로 명명했으며 기존 32비트 `Object size/ObjClass/HealthData/UpdateData=780/8/556/748`, 64비트 `928/12/616/872`, `PlayerUpdateData size=556/640`과 뒤 pointer 필드 배치를 유지한다. raw C 본체는 provenance-only이고 active ABI는 `void nox_xxx_playerHP_4EE730(nox_object_t*)`다. sole production caller는 typed export를 사용하지만 그 caller의 `v3` 객체 생산자는 여전히 ABI32 정수이므로 상위 player-file loader widening 범위에 남긴다.

clean revision `6ea3d7459c04827266245d549263d09783a623a4`에서 Go 1.26.5 macOS/ARM64 표적 10회·race/checkptr 각 3회·전체 server 3회와 생성 Mach-O 직접 실행 10회를 통과했다. `server.test` SHA-256은 `76bc00ede69a0da2444e22b488cfc952d888d8e8913240d1f3cb75e384250bbe`이고 원본 75/80바이트 pattern은 모두 0개다. 독립 C11 O0/O2·ASan/UBSan, 생성 CGo export/wrapper의 엄격한 ARM64 객체 컴파일과 전처리된 두 생산 C 번역 단위의 typed 선언/call도 통과했다. 전체 `make oracle-test`는 원본 트리 전후 동일성, 코드 442개·데이터 192개와 NXZ strict 50쌍을 통과했다. 이식성 집계는 `go_layout 1661/230`, `go_pointer_conversion 437/187`, `go_unsafe 3966/494`, `c_static_assert 1250/137`, `x86_isa 112/59`, `c_pointer_integer_cast 625/48`, `unsafe_literal_offset 202/35`, `cgo_import 230/230`이다. 전체 macOS `legacy`는 기존 Win32 고정 layout assertion에서 중단되며 정책에 따라 macOS/AMD64·Linux·Windows와 전체 9-tuple 행렬은 실행하지 않았다. 이 함수는 FoodDrop 기준점 뒤 열두 번째 ARM64-only 단위 `12/19`, 다음 전체 행렬 전 남은 단위는 7개이고 다음 순차 감사 대상은 current-HP getter `004EE780`이다.

원본 `nox_xxx_unitGetHP_4EE780`은 `004EE780..004EE799`의 정확히 26바이트이고 `004EE79A..004EE79F`는 6바이트 NOP이며 다음 maximum-HP getter가 `004EE7A0`에서 시작한다. body·padding·결합 32바이트 SHA-256은 각각 `c842830466581834b537f438907e121a61547ce43c6d07ef033207e13970b674`, `ff35ffe14925642da6f3a258b35811e08101c03f8b5db346e5afcca448677564`, `da588282857ebdd461948726c73bdf2226799a787d075cab656b80b6636a46b8`이다. raw `.text` rel32 scan과 명령 디코드는 direct call `0041A34A/004E0229/004E080E/004E090E/004E28A8/004E28D1/004EBD89/004F6737/004F69F3/00501564/0052DD65/0052DEE3/0052F3AD/0052F51B/0052F536/0052F75A/0056FB76` 17곳과 direct jump 0곳에 일치하며 whole-file little-endian absolute entrypoint도 0개다. `004EBD89`는 이미 봉인된 UndeadKiller 본체 안에 있고 나머지 16개 call instruction은 독립 범위로 추가했다.

원본은 stack argument를 한 번 읽고 nil이면 zero AX를 반환한다. non-nil이면 객체의 HealthData를 정확히 한 번 읽어 cache하고, nil이면 zero AX를 반환하며, non-nil이면 cached record의 Cur word를 정확히 한 번 읽어 반환한다. generic 계약은 이 세 관찰 이벤트와 모든 fault prefix, HealthData pointer와 Cur 값의 cache, `0x0000/0x7fff/0x8000/0xffff` 전체 word 보존을 고정한다. 내부 Go 값은 `uint16`, public C ABI는 같은 저위 16비트를 반환하는 `short nox_xxx_unitGetHP_4EE780(nox_object_t*)`다. 누적 오라클은 **코드 460개·데이터 192개**이고 다음 순차 함수는 maximum-HP getter `004EE7A0`이다.

구현 결속은 `2b296d1d8/2ea35647c/a89ae0239/9f1327ce6/ba55b9a61`로 오라클·순수 의미·native server·legacy 전환·C ABI를 분리했다. `Object size/HealthData`는 32비트 `780/556`, 64비트 `928/616`이고 `HealthData size/Cur`는 모든 폭에서 `20/0`이다. raw C 본체는 provenance-only이며 원본 `004EBD89` 호출을 소유한 native UndeadKiller는 Go getter를 직접 사용한다. 전처리된 여섯 생산 C 번역 단위에는 typed 선언과 나머지 16개 활성 call만 남는다. 그중 checksum caller `0056FB76`은 이미 native object pointer를 넘기지만 나머지 15개 call instruction의 상위 C 객체 생산자는 ABI32 정수이므로 각 caller widening 범위에 남긴다.

clean revision `ba55b9a6119f48172b692266eecd93c575e26283`에서 Go 1.26.5 macOS/ARM64 표적 10회·race/checkptr 각 3회·전체 server 3회와 생성 Mach-O 직접 실행 10회를 통과했다. `server.test` SHA-256은 `c1b66425ddde9917adae532d5dd7dd4c99c8c60f833cb966b4af488e68bc22f0`이고 원본 26/32바이트 pattern은 모두 0개다. 독립 C11 O0/O2·ASan/UBSan, 생성 CGo header의 exact `extern short`와 export/wrapper의 엄격한 ARM64 객체 컴파일도 통과했다. 전체 `make oracle-test`는 원본 트리 전후 동일성, 코드 460개·데이터 192개와 NXZ strict 50쌍을 통과했다. 이식성 집계는 `go_layout 1667/231`, `go_pointer_conversion 438/188`, `go_unsafe 3972/495`, `c_static_assert 1255/138`, `x86_isa 112/59`, `c_pointer_integer_cast 625/48`, `unsafe_literal_offset 202/35`, `cgo_import 231/231`이다. 전체 macOS `legacy`는 새 export에 도달하기 전 기존 Win32 고정 layout assertion 28개에서 중단되며 정책에 따라 macOS/AMD64·Linux·Windows와 전체 9-tuple 행렬은 실행하지 않았다. 이 함수는 FoodDrop 기준점 뒤 열세 번째 ARM64-only 단위 `13/19`, 다음 전체 행렬 전 남은 단위는 6개이고 다음 순차 감사 대상은 maximum-HP getter `004EE7A0`이다.

원본 `nox_xxx_unitGetMaxHP_4EE7A0`은 `004EE7A0..004EE7BA`의 정확히 27바이트이고 `004EE7BB..004EE7BF`는 5바이트 NOP이며 다음 maximum-HP setter가 `004EE7C0`에서 시작한다. body·padding·결합 32바이트 SHA-256은 각각 `f45238ed5aa92034eef46ed574b2967bc0595a25e2a1d3b149fe6bc20f6475d6`, `18e800921eac4b6ea289ffc28abb7e2d58e7521d3568dcacd9e3aa55096f35de`, `7068d14358f49ab71ef53a4b9ae32a6ff8c0e7b75687d59ead039e13c056b951`이다. raw `.text` rel32 scan과 명령 디코드는 direct call `0041A433/0041AAD3/004E0220/004E0261/0052DD6E/0052F3A1` 6곳과 direct jump 0곳에 일치하며 whole-file little-endian absolute entrypoint도 0개다. 여섯 call instruction을 각각 독립 코드 범위로 봉인했다.

원본은 stack argument를 정확히 한 번 읽고 nil이면 zero AX를 반환한다. non-nil이면 객체의 HealthData를 정확히 한 번 읽어 cache하고, nil이면 zero AX를 반환하며, non-nil이면 cached record의 `+4` Max word를 정확히 한 번 읽어 반환한다. generic 계약은 세 관찰 이벤트와 모든 fault prefix, HealthData pointer와 Max 값의 cache, `0x0000/0x7fff/0x8000/0xffff` 전체 word 보존을 고정한다. 내부 Go 값은 `uint16`, public C ABI는 같은 저위 16비트를 반환하는 `short nox_xxx_unitGetMaxHP_4EE7A0(nox_object_t*)`다. 구현 결속은 `7c53e72f5/237dfa5da/eb9aa3af9/33e80079c/ed06f34d2`로 오라클·순수 의미·native server·legacy 전환·C ABI를 분리했다. `Object size/HealthData`는 32비트 `780/556`, 64비트 `928/616`이고 `HealthData size/Max`는 모든 폭에서 `20/4`다. raw C 본체는 provenance-only이며 전처리된 생산 C 번역 단위에는 typed 선언과 여섯 활성 call만 남는다. 여섯 호출자의 상위 객체 생산자는 모두 ABI32 정수이므로 각 caller widening 범위에 남긴다. 누적 오라클은 **코드 468개·데이터 192개**이고 다음 순차 함수는 maximum-HP setter `004EE7C0`이다.

clean functional revision `ed06f34d27a9fc9c79a81aaf32757668f71991d0`에서 Go 1.26.5 macOS/ARM64 표적 10회·race/checkptr 각 3회·전체 server 3회와 생성 Mach-O 직접 실행 10회를 통과했다. `server.test` SHA-256은 `65928a48f911f88cba4a60dfb45eb9abac525f656e36cd1a70e8d8a0bb421dbf`이고 원본 27/32바이트 pattern은 모두 0개다. 독립 C11 O0/O2·ASan/UBSan, 생성 CGo header의 exact `extern short`와 export/wrapper의 엄격한 ARM64 객체 컴파일도 통과했다. 전체 `make oracle-test`는 원본 트리 전후 동일성, 코드 468개·데이터 192개와 NXZ strict 50쌍을 통과했다. 이식성 집계는 `go_layout 1673/232`, `go_pointer_conversion 439/189`, `go_unsafe 3978/496`, `c_static_assert 1260/139`, `x86_isa 112/59`, `c_pointer_integer_cast 625/48`, `unsafe_literal_offset 202/35`, `cgo_import 232/232`다. 전체 macOS `legacy`는 새 export에 도달하기 전 기존 Win32 고정 layout assertion 28개에서 중단되며 정책에 따라 macOS/AMD64·Linux·Windows와 전체 9-tuple 행렬은 실행하지 않았다. 이 함수는 FoodDrop 기준점 뒤 열네 번째 ARM64-only 단위 `14/19`, 다음 전체 행렬 전 남은 단위는 5개이고 다음 순차 감사 대상은 maximum-HP setter `004EE7C0`이다.

원본 `nox_xxx_unitSetMaxHP_4EE7C0`은 `004EE7C0..004EE7DB`의 정확히 28바이트이고 `004EE7DC..004EE7DF`는 4바이트 NOP이며 다음 poison activation이 `004EE7E0`에서 시작한다. body·padding·결합 32바이트 SHA-256은 각각 `99809376e71f04edd47843afaacdbae559b0e0944bccad5b4bb85d5fc06d780e`, `e61d6a793b42951d4e466a18683567c9011cd840b03559c0cc9e94c761995098`, `75030a4cbc76a4b7a0931abf1c9410eb3718a4abfaaa96fa494e8c0c7545c90d`이다. raw `.text` rel32 scan과 명령 디코드는 player transfer의 sole direct call `0041AAF9` 한 곳, direct jump 0곳에 일치하며 whole-file little-endian absolute entrypoint도 0개다. call instruction SHA-256은 `dfc1633e9d015d14f867e8c9b9add4e1929aa807c676935ea57b41849c7f76a0`이다.

원본은 stack object argument를 한 번 읽고 nil이면 null EAX를 반환한다. non-nil이면 객체의 HealthData를 한 번 읽어 cache하고 nil이면 null을 반환한다. 두 gate가 통과한 뒤에만 stack maximum argument의 저위 word를 읽어 cache하고 cached HealthData의 `+4`에 한 번 저장한 뒤 같은 HealthData pointer를 반환한다. generic 계약은 object→HealthData→maximum→store의 네 관찰 이벤트와 모든 fault prefix, 세 값의 cache, `0x0000/0x7fff/0x8000/0xffff` 전체 word와 반환 identity를 고정한다. 구현 결속은 `34c3b4c80/c0730633b/8067baeaf/c5627807b/f202b5ad9`로 오라클·순수 의미·native server·legacy 전환·C ABI를 분리했다. `Object size/HealthData`는 32비트 `780/556`, 64비트 `928/616`이고 `HealthData size/Max`는 모든 폭에서 `20/4`다. raw C 본체는 provenance-only이며 active ABI는 `void* nox_xxx_unitSetMaxHP_4EE7C0(nox_object_t*, short)`다. 전처리된 생산 C 번역 단위에는 typed 선언과 반환을 버리는 sole active call만 남으며 상위 객체 생산자는 ABI32 정수이므로 caller widening 범위에 남긴다. 누적 오라클은 **코드 471개·데이터 192개**이고 다음 순차 함수는 poison activation `004EE7E0`이다.

clean functional revision `f202b5ad98022f3e6ffc18fad58a74ca3ab64027`에서 Go 1.26.5 macOS/ARM64 표적 10회·race/checkptr 각 3회·전체 server 3회와 생성 Mach-O 직접 실행 10회를 통과했다. `server.test` SHA-256은 `0ab496525146e74ef54180d4ca4b93500d5228189e0ee2485850be84898ca2cc`이고 원본 28/32바이트 pattern은 모두 0개다. 독립 C11 O0/O2·ASan/UBSan, 생성 CGo header의 exact `extern void*`와 export/wrapper의 엄격한 ARM64 객체 컴파일도 통과했다. 전체 `make oracle-test`는 원본 트리 전후 동일성, 코드 471개·데이터 192개와 NXZ strict 50쌍을 통과했다. 이식성 집계는 `go_layout 1681/233`, `go_pointer_conversion 440/190`, `go_unsafe 3988/498`, `c_static_assert 1265/140`, `x86_isa 112/59`, `c_pointer_integer_cast 625/48`, `unsafe_literal_offset 202/35`, `cgo_import 233/233`이다. 전체 macOS `legacy`는 새 export에 도달하기 전 기존 Win32 고정 layout assertion 28개에서 중단되며 정책에 따라 macOS/AMD64·Linux·Windows와 전체 9-tuple 행렬은 실행하지 않았다. 이 함수는 FoodDrop 기준점 뒤 열다섯 번째 ARM64-only 단위 `15/19`, 다음 전체 행렬 전 남은 단위는 4개이고 다음 순차 감사 대상은 poison activation `004EE7E0`이다.

`oracle-test`는 의미 비교 전과 후에 O0과 코드 범위 검증을 실행한다. 테스트 입력은 읽기 전용으로 취급해야 하며, 컨테이너/VM에서는 가능하면 `nox/`를 read-only로 마운트한다. 사후 검사는 잘못된 테스트가 원본을 수정한 경우도 실패로 남긴다.

다른 위치의 정당한 보유본을 쓰려면 절대 경로나 저장소 루트 기준 경로를 넘긴다.

```sh
make oracle-verify NOX_ORACLE_ROOT=/absolute/path/to/nox
```

Windows에서 GNU Make를 사용하지 않는 경우에도 같은 검증기를 실행할 수 있다.

```powershell
$NoxRoot = (Resolve-Path 'C:\Games\Nox').Path
$Manifest = (Resolve-Path '.\toolchain\oracle\nox-2023-1003-01.json').Path
.\scripts\go.ps1 -C src run .\internal\noxoracle verify `
  -root $NoxRoot -manifest $Manifest

$CodeManifest = (Resolve-Path '.\toolchain\oracle\game-exe-functions.json').Path
.\scripts\go.ps1 -C src run .\internal\noxoracle code-verify `
  -root $NoxRoot -manifest $CodeManifest -oracle-manifest $Manifest
```

전체 트리 매니페스트는 원본의 **신원과 무결성**을 보장하는 Gate O0이다. 함수 범위 매니페스트는 포팅한 소스가 어느 원본 주소·바이트 범위에서 유래했는지 기계적으로 고정하지만, 해시만으로 의미 동등성을 증명하지는 않는다. 각 함수에는 어셈블리 검토와 원본 또는 검증된 기준 구현을 사용한 차등 시험이 추가로 필요하고, 게임 상태·패킷·프레임 동등성은 상위 게이트에서 별도로 검증한다.
